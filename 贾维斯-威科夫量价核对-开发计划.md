# 威科夫×订单流「量价核对」— 开发计划

> 版本：v1.0 | 日期：2026-08-05 | 方案：C（两期交付）| 起草：全栈工程师会话
>
> **目标**：用威科夫（Wyckoff）阶段叙事 × 订单流微观证据的交叉核对，识破假突破、
> 透视主力资金的真实供需底牌，把「散户误判量价逻辑」的痛点做成可量化的系统能力。
>
> **面向执行工程师**：本计划假设你对仓库零上下文。每个任务给出精确文件路径、
> 接口契约、核心判定规则与验收命令。实现顺序 = 任务编号顺序；每任务独立 commit。

---

## 〇、当前进度（断电后从这里接着做）

> 规则：每完成一项，把对应任务的 ⬜ 改成 ✅，并在下方「最近一次改动」补一行摘要。
> 断电/重连/换人后先读本节，找到第一个 ⬜ 的任务接着做。

- **当前阶段**：**P2 后端全量完成 ✅**（T2.1-T2.7；P1 全部完成）
- **下一步要做的任务**：T2.8（P2 验收 + code-audit 收口）
- **已完成任务数**：12 / 13
- **T2.8 验收门禁已就绪（红灯基线，2026-08-05 包丁）**：`_wyckoff_smoketest.py`
  （20 例：区间正/反例、12 事件逐一、阶段状态机递进、防前瞻截断、T2.4 绑定
  公式、500 根性能护栏、T2.7 配置契约）与 `_wyckoff_backtest_smoketest.py`
  （零出网 + insufficient_samples/uplift 口径断言）已按 §T2.1-T2.6 冻结契约
  落盘。当前基线：两冒烟红灯（jarvis_wyckoff 未交付，预期）、`/api/wyckoff`
  404（预期）、desktop vitest 255/255 + tsc 全绿（P1 基线）。甲交付后直接
  复跑 §T2.8 命令即可验收；事件 type 约定小写（sc/ar/st/spring/test/sos/
  lps/bc/ut/utad/sow/lpsy），回测入口函数名候选 backtest/run_backtest。
- **T2.5 前端部分已完成 ✅（2026-08-06 乙/W1-B）**：新建 `desktop/src/lib/wyckoff.ts`
  （冻结契约 TS 类型 + 12 事件 type→图标/配色/方位映射 + `buildWyckoffBand` /
  `buildWyckoffMarks` 纯函数变换）与 `WyckoffPrimitive.ts`（阶段带 bottom 层
  半透明矩形，acc 绿/dist 红、透明度按 Phase A-E 递进；事件缩写徽章 top 层，
  与 TrapSignalsPrimitive 同型 canvas 通道，悬停出置信度/订单流佐证摘要）；
  `client.ts` 增 `wyckoff()`；`Chart.tsx` 增威科夫开关（localStorage 持久化）+
  状态条（阶段徽章/事件数/最新事件/verdict_hint/stale 角标/接口未就绪提示），
  数据获取以「最新 K 线开盘时间」为依赖联动现有 kline 轮询（新 bar 才重拉，
  零新增 setInterval，性能纪律第 6 条达标）。`wyckoff.test.ts` 14 例（契约
  解析/URL/ok:false 降级/404 ApiError/range=null/events 空数组/12 事件全映射/
  吸附与窗口裁剪/未知 type 丢弃/phase 透明度递进）。验证：tsc 全绿 +
  vitest 269/269（基线 255 零回归）。T2.5 后端（`/api/wyckoff` 路由）仍待甲交付。
- **最近一次改动**：2026-08-06 T2.5(后端)/T2.6/T2.7 交付（W2）——
  T2.5 后端：`jarvis_wyckoff.analyze` 增 `_stale_fallback`（取数失败/K线不足/
  引擎异常回上次成功缓存标 `stale:true`，成功响应显式 `stale:false`），
  `/api/wyckoff` 60s 缓存与指纹增量沿用既有骨架，curl 实测 ok:true；
  T2.6 回测：`run_backtest` 输出补顶层 `samples/hits/hit_rate/baseline/uplift`
  聚合契约（uplift 取整后相减保证算术一致），历史不足改走诚实口径（samples=0
  + status=insufficient_samples），单事件小样本改标 `low_samples`（避免与整体
  契约字面冲突），`/api/wyckoff/backtest` 1h 缓存实测 ok:true（500 根出 13
  样本如实标 insufficient_samples）；T2.7 安全带：共识端点按 whale 因子同款
  模式接线 `seatbelt.wyckoff`（默认关零回归，多头×dist-C/D、空头×acc-C/D 出
  警示不阻断）。验证：`_wyckoff_smoketest.py` 48/48（新增 8 例 T2.7 配置
  契约）、`_wyckoff_backtest_smoketest.py` 10/10、`_supply_demand_smoketest.py`
  27/27 零回归、py_compile 全过、stale 回退离线夹具 PASS。
  commit：`69b934f`（T2.5）、`9e00107`（T2.6）、`4d2ebad`（T2.7）。
- **上一次改动**：2026-08-06 T2.1-T2.4 交付（W1-A）——`jarvis_wyckoff.py`
  威科夫引擎核心转绿：T2.1 `detect_range` 边界改为只取候选起点后
  `RANGE_FORM_BARS=40` 根形成期的 swing 点（AR/ST 结构定边界；Spring/UT
  扫针与 markup/markdown 离场尾巴不再改写边界，被污染候选由 inside-ratio
  + 宽度纪律自然淘汰）；T2.2 Spring/UT/UTAD 破位判定增加 `SHAKE_MATURITY=30`
  区间成熟度门禁（SC bar 自身下探、形成期噪声不再误报摇仓事件）；
  T2.3 状态机 / T2.4 enrich 复核无改动即达标。验证：`_wyckoff_smoketest.py`
  40/40 ALL PASS（500 根全链 0.5ms < 50ms 性能纪律达标；防前瞻前缀重放
  acc×4 + dist×3 全过）、`_supply_demand_smoketest.py` 27/27 回归绿、
  CLI `jarvis_wyckoff.py BTCUSDT --interval 1h` live 实测正常出报告。
  commit：`ff4850a`（T2.1）、`53c01e6`（T2.2）。
- **更早一次改动**：2026-08-05 P1 一次性交付——新建 `jarvis_supply_demand.py`
  （六路证据采集 + 裁决纯函数 + 突破核验 + CLI）；`jarvis_delta_flow.fetch_bars`
  切合约优先+现货回退（6 个合约独有品种从此有 K 线/陷阱/CVD 证据）；
  dashboard 挂 `/api/sd-verdict`（60s 缓存）；`jarvis_config` 增 `sd_weights`；
  desktop 新增 `SupplyDemandCard`（挂 Chart 页反转四条件下方 + 盘口透视页阶梯下方，
  盘口页 1m 映射 5m）；`_supply_demand_smoketest.py` 27 例 + `sdVerdict.test.ts` 3 例。
  验证：后端冒烟 27/27、回归（delta_flow 32/trap/config）全绿、前端 tsc + vitest
  255 例全绿、live 实测 BTCUSDT coverage 100%（dashboard 进程内含 WS 证据）、
  SNDKUSDT 封禁期优雅降级 coverage 0。

### 任务总表

| 期 | 任务 | 内容 | 预估 | 状态 |
| --- | --- | --- | --- | --- |
| P1 | T1.1 | 后端 `jarvis_supply_demand.py` 证据采集+裁决纯函数 | 3h | ✅ |
| P1 | T1.2 | dashboard `GET /api/sd-verdict` + 进程内缓存 | 1h | ✅ |
| P1 | T1.3 | 桌面端 `SupplyDemandCard.tsx` 底牌卡（盘口页+K线页） | 3h | ✅ |
| P1 | T1.4 | `_supply_demand_smoketest.py` + 前端 vitest | 2h | ✅ |
| P1 | T1.5 | P1 验收 + code-audit 收口 | 1h | ✅ |
| P2 | T2.1 | `jarvis_wyckoff.py` 交易区间识别（swing+区间） | 3h | ✅ |
| P2 | T2.2 | 威科夫事件检测（12 事件规则引擎） | 4h | ✅ |
| P2 | T2.3 | 阶段状态机（吸筹/派发 Phase A-E） | 3h | ✅ |
| P2 | T2.4 | 事件×订单流证据绑定（置信度加权） | 2h | ✅ |
| P2 | T2.5 | `GET /api/wyckoff` + K线页阶段带/事件标记 | 4h | ✅ |
| P2 | T2.6 | 历史回放回测（命中率 vs 基线，诚实口径） | 3h | ✅ |
| P2 | T2.7 | 安全带因子接入（默认关，零回归） | 1h | ✅ |
| P2 | T2.8 | P2 验收 + code-audit 收口 | 1h | ⬜ |

---

## 一、背景与成功标准

**痛点**：散户（用户本人）经常误判假突破的量价逻辑——看到「放量突破」就追，
实际是主力借突破派发（UTAD）或插针扫损（Spring 的反向）。

**本计划解决**：K 线量价是「影子」，逐笔订单流才是「底牌」。把两者交叉核对：
威科夫事件给出宏观叙事（现在处于吸筹还是派发、哪个事件刚发生），订单流证据
（Delta/CVD/大单净流/盘口吸收/足迹失衡）给出微观实锤，二者一致才给高置信结论。

**成功标准（可量化验收）**：
1. P1：盘口页/K线页出现「主力底牌卡」，任一 watchlist 品种 3 秒内出卡，
   突破场景给出 真突破/假突破嫌疑 + 证据链（每条证据可追溯到来源模块）。
2. P2：K 线图叠加阶段带与事件标记；Spring/UTAD 事件回测命中率显著高于
   同口径基线（`uplift > 0`，样本数 ≥ 30 时才展示结论——与 delta_flow 诚实口径一致）。
3. 性能：新功能新增 REST 出网 ≈ 0（全部复用既有取数+TTL 缓存+WS 内存态）；
   单次裁决计算 < 50ms（bars ≤ 500）；前端不新增独立轮询定时器。

---

## 二、可复用资产盘点（先读这些，别重复造轮子）

| 现有模块 | 提供能力 | 本计划怎么用 |
| --- | --- | --- |
| `jarvis_delta_flow.py` | `fetch_bars(symbol, tf, limit)` 全仓唯一保留 `taker_buy(k[9])` 的取数口；`compute_delta_series`（delta/CVD）；吸收背离检测 | P1/P2 唯一 K 线取数入口；CVD 证据源 |
| `jarvis_trap_detect.py` | 四规则陷阱检测（假突破回落/长影线插针/量价背离/Delta背离），`/api/trap-signals` 契约已定 | P1 证据源之一；P2 事件检测的实现范本（纯函数吃 bars 升序列表） |
| `jarvis_whale_tape.py` | WS 内存态大单分层净流 `summary(symbol)` | P1 证据源（读内存零出网；WS 不在线时返回空=证据缺失） |
| `jarvis_orderbook.py` | 本地订单簿引擎，深度切片（内存+SQLite） | P1 盘口吸收证据（买卖墙、挂单吸收） |
| `jarvis_volume_profile.py` | POC/VAH/VAL、多分布结构、`/api/volume-profile` | P1 证据源（价值区位置）；P2 区间识别的参考实现 |
| `jarvis_crypto_data.py` | `_get`（三道防限频闸：端点 TTL / 封禁短路 / 分钟预算 `rest_max_per_min`） | **硬规矩：任何新取数必须走 `jcd._get` 或 `delta_flow.fetch_bars`，禁止裸 `requests`** |
| `jarvis_config.py` | 配置中心（DEFAULTS/GROUPS/BOUNDS，YAML 热加载） | 新增权重/开关全部进配置，可热调不改码 |
| `jarvis_seatbelt.py` | 开仓安全带可选因子框架（whale/liq_map 已挂） | P2 T2.7 照同款模式挂 wyckoff 因子 |
| `desktop/src/lib/trapSignals.ts` + `KlineChart.tsx` | 前端信号 marker 渲染机制 | P2 事件标记复用同型结构 |
| `desktop/src/hooks/useApi.ts` | 前端统一轮询 hook | P1 卡片数据获取（并入现有刷新节奏） |

**数据现实约束（2026-08-05 现状，写代码前必读）**：
- watchlist 8 品种中 SNDK/SKHY/SPCX/XAU/CL/BZ 为合约独有；当前网络下合约 WS
  （fstream）不通 → 这 6 个品种 whale_tape/orderbook 证据为空，K 线走合约 REST
  （带 30s TTL 缓存）。**裁决层必须支持证据缺失降级**（见 T1.1 coverage 字段）。
- BTC/ETH 有现货 WS 实时流，证据最全。

---

## 三、总体架构

```
数据层（全部复用，零新增出网）
  delta_flow.fetch_bars(REST+TTL) ─┐
  whale_tape.summary(WS内存)      ─┤
  orderbook 深度切片(WS内存)       ─┼──> jarvis_supply_demand.py   [P1]
  volume_profile(REST+TTL)        ─┤      collect_evidence()  六路证据采集（各自降级）
  trap_detect(复用fetch_bars)     ─┘      verdict()           纯函数裁决：供需结论+证据链
                                              │
                                              ├──> /api/sd-verdict ──> 底牌卡（盘口页+K线页）
                                              │
                          jarvis_wyckoff.py  [P2]（单向依赖 supply_demand）
                            detect_range()   交易区间识别
                            detect_events()  12 事件规则引擎
                            resolve_phase()  阶段状态机 A-E
                            enrich()         事件×订单流证据 → 置信度
                                              │
                                              ├──> /api/wyckoff ──> K线阶段带+事件标记
                                              └──> jarvis_seatbelt 可选因子（默认关）
```

**模块边界**：`supply_demand` 只管「此刻的供需证据与裁决」；`wyckoff` 只管
「区间/事件/阶段叙事」，需要微观证据时调用前者——单向依赖，各自独立冒烟。

---

## 四、性能纪律（硬约束，验收要查）

1. **取数**：只走 `delta_flow.fetch_bars` / `jcd._get`（自带端点 TTL 30s、封禁短路、
   分钟预算）与 WS 内存态。任何 PR 出现裸 `requests.get` 打交易所 = 直接打回。
2. **结果缓存**：`/api/sd-verdict` 与 `/api/wyckoff` 按 `(symbol, interval)` 进程内
   缓存 60s（带 `threading.Lock`，模式抄 `jarvis_funding_arb._OPP_CACHE`）。
3. **增量计算**：wyckoff 阶段引擎以「最后一根已收盘 bar 的 open_time」做缓存指纹，
   新 bar 出现才重算，否则直接回缓存。
4. **复杂度上限**：bars ≤ 500；swing 检测 O(n) 滚动窗口；事件扫描 O(n×12)；
   禁止两两配对 O(n²) 写法。
5. **防前瞻**：所有判定只用已收盘 bar（丢弃进行中最后一根）——抄 delta_flow 纪律。
6. **前端**：卡片数据并入页面现有刷新周期（30-60s），不新增 `setInterval`；
   阶段带/事件标记随 K 线接口刷新联动。

---

## 五、Phase 1：量价核对裁决卡（P1，约 1 人日）

### 文件清单

| 文件 | 动作 | 职责 |
| --- | --- | --- |
| `jarvis_supply_demand.py` | 新建 | 证据采集 + 裁决纯函数 + CLI |
| `jarvis_dashboard.py` | 修改 | 挂 `/api/sd-verdict` 路由 + 60s 缓存 |
| `desktop/src/api/client.ts` | 修改 | 增加 `sdVerdict()` 客户端方法 |
| `desktop/src/components/cards/SupplyDemandCard.tsx` | 新建 | 底牌卡组件 |
| `desktop/src/pages/DepthView.tsx`（盘口透视页 /depth）| 修改 | 挂卡 |
| `desktop/src/pages/Chart.tsx` | 修改 | 侧栏挂卡 |
| `_supply_demand_smoketest.py` | 新建 | 离线冒烟（打桩证据） |
| `desktop/src/lib/__tests__/sdVerdict.test.ts` | 新建 | 契约解析单测 |

### T1.1 后端 `jarvis_supply_demand.py`

**证据采集**（六路独立 try/except，单路失败置 None 不拖垮）：

```python
def collect_evidence(symbol: str, interval: str = "15m") -> dict:
    """六路证据采集，全部复用现有模块；单路异常降级 None。
    返回 {"bars": [...], "trap": ..., "cvd": ..., "whale": ...,
          "book": ..., "vp": ..., "coverage": 0.0~1.0}"""
```

- `bars`：`jarvis_delta_flow.fetch_bars(symbol, interval, limit=200)`（唯一取数口）
- `trap`：`jarvis_trap_detect` 对 bars 跑四规则，取最近 10 根内信号
- `cvd`：`jarvis_delta_flow` delta 序列 → 近 20 根 CVD 斜率 + 价格/CVD 背离标记
- `whale`：`jarvis_whale_tape.summary(symbol)`（无 WS → None）
- `book`：`jarvis_orderbook` 最新深度切片 → 前 10 档买卖失衡比（无 WS → None）
- `vp`：`jarvis_volume_profile` 当前价相对最近分布 POC/VAH/VAL 的位置
- `coverage`：非 None 路数 / 6（前端展示「证据覆盖度」，覆盖度低时结论降权）

**裁决纯函数**（离线可测，权重可配置）：

```python
DEFAULT_WEIGHTS = {  # 配置键 sd_weights 覆盖（jarvis_config，JSON 字符串）
    "trap": 0.30, "cvd": 0.25, "whale": 0.20, "book": 0.10, "vp": 0.15,
}

def verdict(ev: dict, weights: dict | None = None) -> dict:
    """证据 → 供需裁决。纯函数：不取数、不联网。
    每路证据先归一到 [-1,+1]（+=需求方/吸筹，-=供给方/派发），加权求和后：
      score >= +0.35 → bias=accumulation（主力吸筹）
      score <= -0.35 → bias=distribution（主力派发）
      其间            → bias=neutral
    confidence = min(1.0, abs(score)) × coverage —— 证据不全自动降置信。"""
```

**突破核验子判定**（痛点 1 的直接回答，规则写进代码注释便于校准）：

- 触发条件：最新已收盘 bar 收盘价突破近 `LOOKBACK=20` 根前高/前低。
- `breakout_check.verdict` 取值与规则：
  - `confirmed`（真突破）：突破方向 delta 同向放大 ≥ 1.5×近20根均值，且 CVD 创同向新高/新低，且无反向大单净流
  - `suspect`（假突破嫌疑）：满足任一——①量能 < 0.8×均值（无承接）②CVD 背离 ③反向 whale 净流 ≥ tier1 ④trap 四规则任一在突破 bar ±3 根内触发
  - `unknown`：无突破或证据覆盖度 < 0.34
- 输出 `reasons: list[str]`（中文人话，直接上卡）。

**返回契约**（同 `/api/sd-verdict` 响应，见 T1.2）。

**CLI**：`python jarvis_supply_demand.py BTCUSDT --interval 15m [--json]`（排障用）。

### T1.2 dashboard API

```
GET /api/sd-verdict?symbol=BTCUSDT&interval=15m
{
  "ok": true, "symbol": "BTCUSDT", "interval": "15m", "ts": 1785920000.0,
  "bias": "accumulation|distribution|neutral",
  "score": 0.42, "confidence": 0.63, "coverage": 0.83,
  "breakout_check": {"active": true, "direction": "up",
    "verdict": "suspect", "reasons": ["突破前高但 CVD 走平（背离）",
    "突破 bar 放量 2.1×，随后两根缩至 0.5×（无跟随）"]},
  "evidence_chain": [
    {"source": "trap", "signal": "bull_trap", "direction": -1,
     "weight": 0.30, "detail": "假突破回落：突破 64800 后 2 根收回"},
    {"source": "cvd", "signal": "divergence", "direction": -1,
     "weight": 0.25, "detail": "价格新高 CVD 未新高"},
    {"source": "whale", "signal": null, "direction": 0, "weight": 0.0,
     "detail": "WS 离线，无大单数据"}
  ],
  "stale": false
}
```

- 路由函数加 `(symbol, interval)` 60s 进程内缓存；数据层失败回上次缓存并标 `stale: true`。
- 挂在 `jarvis_dashboard.py` 现有 API 区（参考 `/api/trap-signals` 的挂法，约 4009 行附近风格）。

### T1.3 桌面端底牌卡 `SupplyDemandCard.tsx`

- 布局四段：①bias 大字徽章（吸筹=绿/派发=红/中性=灰）+ confidence 环形
  ②突破核验条（active 时显示 verdict + reasons 列表）③证据链折叠列表
  （每条：来源图标+方向箭头+detail，direction=0 灰显）④coverage 进度条 + stale 角标。
- 数据：`useApi` 轮询 `/api/sd-verdict`，间隔跟页面现有刷新（盘口页 30s / Chart 页 60s）。
- 挂点：盘口页深度阶梯右侧；`Chart.tsx` 右侧栏（跟随当前 symbol/interval 联动）。

### T1.4 测试

- `_supply_demand_smoketest.py`（不联网，打桩 collect_evidence 的六路输入）：
  ≥8 例——吸筹裁决 / 派发裁决 / 中性 / 单路证据缺失降级 coverage / 全缺失 unknown /
  真突破 confirmed / 假突破 suspect（CVD 背离桩）/ 权重覆盖生效（sd_weights 传入）。
- `desktop/src/lib/__tests__/sdVerdict.test.ts`：契约字段解析、stale/低 coverage 渲染态、
  evidence_chain 空数组容错，共 3 例。

### T1.5 P1 验收（全部命令必须绿）

```bash
cd Vibe-Trading
.venv/bin/python -m py_compile jarvis_supply_demand.py jarvis_dashboard.py
.venv/bin/python _supply_demand_smoketest.py            # ALL PASS
.venv/bin/python jarvis_supply_demand.py BTCUSDT --json  # 出完整 JSON
curl -s "http://127.0.0.1:7899/api/sd-verdict?symbol=BTCUSDT&interval=15m"  # ok:true
cd desktop && npx vitest run                              # 全绿（含新增 3 例）
```
收口：按仓库惯例跑 code-audit 复查（diff 范围 / 调用链 / 契约一致性）。

---

## 六、Phase 2：威科夫阶段引擎（P2，约 3-4 人日，依赖 P1 的证据接口）

### 文件清单

| 文件 | 动作 | 职责 |
| --- | --- | --- |
| `jarvis_wyckoff.py` | 新建 | 区间/事件/阶段状态机/证据绑定 + CLI |
| `jarvis_dashboard.py` | 修改 | `/api/wyckoff`、`/api/wyckoff/backtest` |
| `desktop/src/lib/wyckoff.ts` | 新建 | 契约类型 + 阶段带/标记数据变换 |
| `desktop/src/pages/Chart.tsx` + `KlineChart.tsx` | 修改 | 阶段背景带 + 事件 marker |
| `jarvis_config.py` | 修改 | `wyckoff_seatbelt_enabled=False` 等键 |
| `jarvis_seatbelt.py` | 修改 | 可选因子挂载 |
| `_wyckoff_smoketest.py` / `_wyckoff_backtest_smoketest.py` | 新建 | 离线冒烟/回测口径 |

### T2.1 交易区间识别

```python
def detect_range(bars: list) -> dict | None:
    """近端交易区间：swing 高低点（k=5 滚动窗口，O(n)）聚类；
    区间成立 = 最近 ≥30 根内，价格 ≥80% 时间落在 [low, high] 且
    (high-low)/ATR14 ≤ 8（横盘密集，非趋势段）。
    返回 {"high","low","mid","start_ts","bars_in_range","atr"} 或 None（趋势段）。"""
```

### T2.2 事件检测（12 事件，规则引擎）

每个事件一条纯函数规则（价格条件+量能条件+区间位置），全部吃已收盘 bars + range：

| 事件 | 侧 | 核心规则（量价） |
| --- | --- | --- |
| SC 恐慌抛售 | 吸筹 | 大阴线 2×ATR + 量 ≥2.5×均量 + 收在 bar 下 1/3 之外（下影承接） |
| AR 自动反弹 | 吸筹 | SC 后 ≤10 根内反弹 ≥0.5×SC 振幅 |
| ST 二次测试 | 吸筹 | 回踩 SC 低点 ±0.5×ATR，量 <0.7×SC 量 |
| Spring 弹簧 | 吸筹 | 破区间低点后 ≤3 根收回区间内；破位量 <均量（无供给跟随）为佳 |
| Test 测试 | 吸筹 | Spring 后回踩不破 Spring 低点且缩量 |
| SOS 强势信号 | 吸筹 | 放量 ≥1.5×均量突破区间中轨/高点且收在 bar 上 1/3 |
| LPS 最后支撑 | 吸筹 | SOS 后回踩缩量不破突破位 |
| BC 抢购高潮 | 派发 | SC 镜像（大阳+巨量+上影） |
| UT 上冲回落 | 派发 | 破区间高点 ≤3 根收回 |
| UTAD 派发上冲 | 派发 | Phase C 位置的 UT + 量价背离（放量滞涨或 CVD 背离） |
| SOW 弱势信号 | 派发 | 放量跌破区间中轨/低点 |
| LPSY 最后供给 | 派发 | SOW 后无力反弹（缩量 + 高点降低） |

`detect_events(bars, rng) → [{"type","ts","price","bar_idx","raw_score"}]`，
`raw_score` 0-1 = 规则满足度（每条规则给主条件 0.6 + 加分项各 0.2）。

### T2.3 阶段状态机

```python
def resolve_phase(events: list, rng: dict) -> dict:
    """事件序列 → {"side": "acc|dist|trend|unknown", "phase": "A|B|C|D|E",
    "since_ts": ...}。推进规则：
      A = SC/BC 出现；B = AR+ST 完成；C = Spring/UTAD 出现；
      D = SOS/SOW 确认；E = LPS/LPSY 后离开区间。
    容错：事件缺失允许跳档但 side 置信降一档；无区间 → trend/unknown。"""
```

### T2.4 事件×订单流证据绑定（依赖 P1）

- 对最近事件窗口（事件 bar ±5 根）调 `jarvis_supply_demand.collect_evidence` +
  `verdict`，方向一致 → `confidence = min(1, raw_score × (1 + 0.5×sd_confidence))`，
  方向相逆 → `confidence = raw_score × 0.4`（叙事与实锤打架时不给高分）。

### T2.5 API + 前端

```
GET /api/wyckoff?symbol=&interval=
{ "ok": true, "symbol": "...", "interval": "...",
  "range": {"high":..., "low":..., "start_ts":...} | null,
  "state": {"side":"acc","phase":"C","since_ts":...},
  "events": [{"type":"spring","ts":...,"price":...,"confidence":0.78,
              "sd_bias":"accumulation","reasons":["..."]}],
  "verdict_hint": "Phase C 弹簧已确认 + 吸筹证据链支持：等待 SOS/LPS 入场结构" }
```

- 前端：阶段带 = K 线背景半透明色带（acc 绿 / dist 红，透明度按 phase 深浅）；
  事件 marker 复用 `trapSignals.ts` 的 marker 通道（新增 type→图标映射）；
  数据随现有 K 线刷新联动，不加新定时器。

### T2.6 历史回放回测（诚实口径，抄 delta_flow）

- 逐 bar 滚动重放 `detect_events`；Spring/LPS 触发后 N=12 根收盘 > 触发价计命中，
  UTAD/LPSY 镜像；基线 = 全体可评估 bar 同口径概率；输出命中率/基线/uplift/样本数。
- 样本 <30 输出 `"insufficient_samples"`，禁止小样本吹牛。
- `/api/wyckoff/backtest?symbol=&interval=&days=90`（供权重校准，结果缓存 1h）。

### T2.7 安全带因子（零回归）

- `jarvis_config.py`：DEFAULTS 加 `"wyckoff_seatbelt_enabled": False`（GROUPS→signal）；
- `jarvis_seatbelt.py`：开关开启且 `/api/wyckoff` 状态为 dist-C/D 时，对多头开仓输出
  提醒条目（模式抄 `whale_seatbelt_enabled` 因子），反向镜像。默认关闭，不影响现网。

### T2.8 P2 验收

```bash
.venv/bin/python _wyckoff_smoketest.py             # ≥12 例 ALL PASS（每事件至少 1 例）
.venv/bin/python _wyckoff_backtest_smoketest.py    # 口径断言 ALL PASS
curl -s "http://127.0.0.1:7899/api/wyckoff?symbol=BTCUSDT&interval=1h"
cd desktop && npx vitest run && npx tsc --noEmit   # 全绿
```
收口：code-audit（重点：契约一致性、防前瞻、性能纪律第 1/3/4 条）。

---

## 七、并行分工建议（给派工用）

| 工程师 | 任务包 | 依赖 |
| --- | --- | --- |
| 甲（后端） | T1.1 → T1.2 → T1.4(py) | 无 |
| 乙（前端） | T1.3 → T1.4(ts)（契约见 T1.2，可与甲并行） | 契约冻结即可动工 |
| 甲或丙 | T2.1 → T2.2 → T2.3（纯函数，可先行） | 不依赖 P1 上线 |
| 甲 | T2.4 → T2.5(后端) → T2.6 → T2.7 | 依赖 T1.1 接口 |
| 乙 | T2.5(前端) | 依赖 T2.5 后端契约 |

commit 规范沿用仓库现状（`feat: xxx`），每任务一 commit。

## 八、风险与降级

1. **6 个合约品种无 WS**（当前网络）：whale/book 证据为空 → coverage 自动降权、
   卡片明示「证据覆盖 50%」；WS 恢复后零改动自动补全。
2. **封禁期**（fapi IP 限频）：取数层自动走缓存 → 响应带 `stale: true`，卡片显示角标。
3. **新品种历史短**（SNDK 等）：bars < 120 → range 返回 None，卡片显示「样本不足」。
4. **阈值初版拍脑袋**：全部权重/阈值进配置中心（`sd_weights` 等），T2.6 回测数据
   出来后校准，改配置即生效不改码。
5. **性能回归**：验收含 50ms 断言（冒烟里 `time.perf_counter` 包一层）。

## 九、明确不做（YAGNI）

- 不做 LLM 解读版底牌卡（已有 ai_explain 通道，后续想加走该通道）
- 不做跨周期共振矩阵（先把单周期做准）
- 不做自动下单联动（只做安全带提醒因子，交易决策链不动）
- 不新增任何交易所 REST 端点依赖
