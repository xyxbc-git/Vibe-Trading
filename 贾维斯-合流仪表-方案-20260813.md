# 贾维斯「盘上实时合流仪表」方案（Confluence HUD）

> 版本：v-final（主控已裁决定稿） | 日期：2026-08-13
> 主笔：agent-8（数据/裁决派）× agent-6（UI/交互派）对抗互评合并，四分歧点主控裁决见 §七
> 状态：**方案文档，未写代码**——工作量估算见 §八，供用户拍板后拆单实施。

---

## 〇、一句话定位

把导师证据引擎（`jarvis_trade_mentor.build_evidence` 七路证据 + 军规上下文）做成
**盘面常驻、随 K 线周期联动的事前扫描视图**：CP1 给你「图上挂片」，我们给你
「挂片 + 你自己的战绩 + 成本可行性 + 行为风控 + 一键进裁决闭环」。

**与导师/军规的关系（零重复建设）**：

| 层 | 回答的问题 | 时机 | 数据 |
| --- | --- | --- | --- |
| 合流 HUD（本方案） | 现在这个 symbol×TF 的环境值得看吗 | 事前扫描 | build_evidence 缓存链 + 军规 ctx |
| 导师裁决（已上线） | 我这个具体计划能不能开 | 事中把关 | 同一引擎 + 计划参数 + 军规逐条 |
| 台账复盘（已上线） | 我听劝有没有用 | 事后信任回路 | mentor_plan + stats |

HUD 点击「按当前合流开计划」→ 预填导师计划表单（方向/TF/现价 entry +
tpsl_advisor 分布建议 SL/TP）→ 完整红黄绿裁决 → 台账。**事前→事中→事后闭环，
这是防止 HUD 变成又一个唇彩面板的结构性设计**：看到高分不能直接下单，
必须过裁决；HUD 高分 + 导师红灯的组合本身就是教育点（环境好 ≠ 这单能开）。

---

## 一、与 CP1 逐项对比（「我们比他多什么」）

| 维度 | CP1（TV 付费指标） | 我们 |
| --- | --- | --- |
| HTF 多周期一致 | ✅ 有（口径不明的黑盒） | ✅ 12 系统 × 5 周期加权共识，口径开源可审计 |
| BOS 结构突破 | ✅ 有 | ✅ 威科夫事件（Spring/UT/SOS）+ 道氏/缠论结构，带阶段语境 |
| Liquidity Sweep | ✅ 有 | ✅ stop_hunt 五条件（刺破+收回+长影+量能+ATR 深度受控） |
| Imbalance/FVG | ✅ 有 | ✅ jarvis_fvg zones + 折价/溢价区（N2 dealing range） |
| Trade Score | ✅ 75 这样的黑盒数字 | ✅ 预登记权重公开 + 每条目可点开人话证据 |
| 逐笔订单流确认 | ❌ | ✅ Delta 吸收/背离三态（seatbelt）——量价是影子，订单流是底牌 |
| 宏观事件风险 | ❌ | ✅ CPI/非农/利率决议倒计时（金十日历），窗口内明示插针风险 |
| 成本可行性 | ❌（指标世界里没有手续费） | ✅ 过路费地板 fee/R 估算——433 笔真实亏损 72.4% 是摩擦的教训 |
| 你自己的战绩 | ❌（通用指标，人人一样） | ✅ 该格子 MFE/MAE 分布、时段真实胜率、听劝 vs 不听劝对比 |
| 行为风控 | ❌（指标不管你上头） | ✅ 冷静期倒计时/当日单数/连亏 streak 徽章（军规 ctx 直读） |
| 数据不足时 | 永远显示信号（卖点就是永远有信号） | 诚实降级：skipped 灰显 + 覆盖率不足分数灰显（预登记纪律） |
| 信号之后呢 | 自己拍脑袋 | 一键预填计划 → 红黄绿裁决 → 台账复盘信任回路 |

---

## 二、条目矩阵（数据源全复用现有缓存链，零新增出网）

> 展示分两级（采 agent-6 意见，见 §六）：**主行恒显 + 展开细项**。
> 每条目四态：pass（绿）/ warn（黄）/ fail（红）/ skipped（灰=数据缺失，不计分母）。

| # | 条目 | 数据源（缓存） | 判定口径 | 展示层级（v2 合并） |
| --- | --- | --- | --- | --- |
| C1 | HTF 多周期一致 | /api/twelve/consensus（180s TTL） | 30m/1h/4h 与 HUD 方向同向数 ≥2 pass；1 warn；0 fail | 折叠态四勾之一 |
| C2 | 入场时机（LTF） | 同 C1 的 tfs.5m | 5m 与方向同向 pass，反向 warn | 展开·方向合流组 |
| C3 | 结构语境 BOS | jarvis_wyckoff（bar 指纹缓存） | acc/dist 阶段与方向一致 + 近期 SOS/Spring/UT 事件 | 折叠态四勾之一 |
| C4 | 流动性扫单 | jarvis_stop_hunt（kline TTL） | 检出同向扫单（long-stops-swept 支持做多）pass | 折叠态四勾之一 |
| C5 | FVG/折溢价 | jarvis_fvg（kline 缓存） | 同向未回补 FVG 存在 + Discount 区做多/Premium 区做空 pass | 折叠态四勾之一 |
| C6 | Delta 微观确认 | jarvis_delta_flow + seatbelt | confirm pass / conflict warn（strong→fail）/ idle 中性 | 展开·微观确认组 |
| C7 | 反转四条件 | stop_hunt.aggregate_reversal_score | satisfied ≥3 加分项（逆势场景的结构证据） | 展开·微观确认组（收编 ReversalScorePanel） |
| C8 | 事件风险窗口 | jarvis_event_calendar（disk TTL） | 窗口外 pass；窗口内 warn + 倒计时；未配置 skipped | 展开·环境健康组 |
| C9 | 资金费拥挤 | jarvis_sentiment（intel TTL） | ｜费率｜≤0.05%/8h pass；追拥挤方向 warn | 展开·环境健康组 |
| C10 | 过路费可行性 | jarvis_config + tpsl_advisor JSON | 该 TF 中位计划 R 下 fee/R ≤0.2 pass；>1.0 强制红标（标「估算」，不进分） | 展开·环境健康组徽章 |
| C11 | 你的格子战绩 | tpsl_advisor JSON + signal_winrate 缓存 | 展示型：MFE/MAE P50/P90、净胜率；n<30 显「不可判定」 | 展开·页脚区 |
| C12 | 行为闸口 | mentor `_rules_context`（台账） | 冷静期=行动闸门（预填按钮禁用+倒计时 tooltip）；当日 N 单=页脚小字；连亏不上盘面 | 行动路径（不进常驻视野） |

> C2/C3/C5 在图上已有对应叠加层（BOS 结构线 / trap 标记 / FVG 带+折溢价线）——
> **HUD 是索引，叠加层是证据**：展开态行点击临时点亮对应层 3 秒（不持久改用户
> 的 persistedToggles），不在 HUD 里复制证据细节。

---

## 三、评分公式（预登记，与导师裁决分明确区分）

**HUD 分 = 环境合流分（setup quality）**，回答「这个环境值得看吗」；
**导师分 = 计划裁决分（plan quality）**，回答「你这个具体计划能不能开」。
两者共享证据但**不共享分数**——HUD 没有 entry/SL/TP，风险数学项无法真判。

```
方向判定：C1 的多周期加权共识方向（neutral 时 HUD 整体置灰显「无方向共识」）

环境合流分（0-100，按可用权重归一）：
  方向合流 40 = 30m/1h/4h 同向各 10 + 5m 时机 5 + 威科夫语境 5
  结构证据 30 = BOS 10 + 扫单 10 + FVG/折溢价 10
  微观确认 20 = Delta 三态 12 + 反转四条件 8
  环境健康 10 = 事件窗口外 5 + 资金费不极端 5

纪律（对齐 verdict 预登记哲学）：
  · skipped 条目不计分母，可用权重 <50/100 → 分数灰显挂「证据不足」
  · 行为徽章（C12）与成本估算（C10）不进分——环境是市场的，行为是你的，
    成本依赖具体 SL；混进去会让分数既不反映市场也不反映纪律
  · C10 fail（该 TF 典型 R 下过路费 >1.0）→ 分数旁强制挂「⚠ 成本不可行」
    红标（不扣分但必须可见——这是 433 笔台账最贵的一课）
```

---

## 四、实时性设计（不加重请求）

- **新增一个聚合端点** `GET /api/confluence?symbol=&tf=`：进程内依次读各路
  **既有缓存**（consensus/wyckoff/delta/hunt/fvg/sentiment/event/军规 ctx），
  聚合评分后自身 `_cached` 30-60s。
- 各路缓存未命中时触发该路自己的取数与限频（与现有页面行为一致），
  **本端点自身零新增出网**。
- 前端随 K 线页**现有轮询节奏**取该端点，切 TF/切 symbol 参数联动；
  不新开轮询定时器。
- 封禁/降级期：各路 skipped 灰显——HUD 半灰是诚实，不是缺陷。

---

## 五、诚实纪律（降级语义）

1. 条目级：skipped 灰显 + 原因一句话（「事件日历未配置」「K 线降级中」）。
2. 分数级：可用权重 <50 → 分数灰显 + 「证据不足」徽章，不给可交易暗示。
3. 战绩级：格子 n<30 显「不可判定（还差 k 笔）」——绝不用小样本给胜率。
4. 全局：HUD 永不显示「买/卖」按钮字样，只显示「按当前合流开计划」进裁决。
   方向标签措辞用「环境偏多/偏空」，不用「BUY/SELL」（与 CP1 的关键差异：
   我们不制造「指标让我买的」的甩锁心理）。
5. **跨模块边界（主控裁决）：「一处结论、一处引用、显式冲突态」**——
   方向/开单级结论只由 HUD 与导师输出；其它模块（如盘口成交流画像）作为
   HUD 证据源被**引用**，其自身结论句限于描述行为（吸收/派发/失衡），
   不给方向建议；被引用证据与 HUD 方向矛盾时，对应证据行显式标
   「⚡冲突」态（不静默吞掉，也不擅自改方向）。

---

## 六、UI 布局（agent-6 v2 稿，基于 Chart.tsx/KlineChart 实地取证）

**位置**：图表容器内**左上角**（右上被 TrapReasonCard 占、右缘是价格轴+折溢价带
+预测锥的最高密度区、左上覆盖的是最老历史 K 线=价值最低区）。
既有「加载更早 K 线」提示从 top-2 left-2 迁至左下（语义更合，一行改动）。

**折叠态**（常驻单行 ~28px，左→右）：
`[▲环境偏多] [4H] [75] [✓✓◐○] [🔒仅冷静期显示] [⌄]`
- 徽章只留两枚：方向（▲绿/▼红/＝灰，沿用 tone 体系）+ TF（中性）；
  折溢价不单独出徽章（它是条件不是方向），作为四勾之一
- 四勾 = CP1 对齐四大项（C1 HTF / C3 BOS / C4 扫单 / C5 FVG 折溢价）
- 🔒只在冷静期出现且不带数字（行为不进常驻视野，只进行动路径）

**展开态**（w-72 卡片、max-h 60% 图高、bg-jarvis-card/85 + backdrop-blur，
ESC/点外部收回）：四组 = 方向合流(40) / 结构证据(30，行点击→图上临时点亮
对应叠加层 3s) / 微观确认(20，收编 ReversalScorePanel 四条件) / 环境健康
(10，含事件倒计时 + fee/R 估算红标)；页脚 = 「预填导师计划」主按钮（冷静期
禁用态+倒计时 tooltip）/ 当日已提交 N 单小字 / 数据新鲜度「xx 秒前」。

**微观确认组的引用/冲突语义（主控补充裁决落地）**：本组的 Delta/盘口成交流
证据均为对其它模块的**引用**——引用行只展示该模块的行为描述原句（如
「卖方吸收，强度 strong」），方向判断由 HUD 统一给出；当引用证据与 HUD
方向矛盾（如 HUD 环境偏多但画像显示派发主导）时，该行标「⚡冲突」并计为
warn/fail 进分，绝不静默丢弃——冲突本身就是最有价值的信息。

**防噪三条**：
1. 分数滞回——变化 <5 分不重绘显示值（防 30-60s 缓存刷新心电图效应）
2. 分段色对齐 ReversalScorePanel 惯例：≥70 金 / 40-69 蓝 / <40 灰——
   **不用红**（红在本项目 tone 体系=空方向/风险，低分用红会被误读为看空）
3. 数据未就绪保持上一状态+角落小 spinner，绝不整卡骨架闪；变化 300ms 淡入；
   尊重 prefers-reduced-motion；禁红色闪烁；分数变化不推通知不发声
   （报警职责归 price_alert/signal_alert 既有链路）

**三态图标**沿用 ReversalScorePanel 惯例（CheckCircle2 绿 / CircleDashed 黄 /
Circle 灰）+ 琥珀⚠区分「取数失败」与「源未接」，不发明第四套图标语言。

**预填跳转**：主方案 = Chart 页内右侧抽屉复用 PlanForm 组件（盘面上下文不丢），
预填 symbol/direction + reasonText（合流快照：分数+四项状态+TF）+ tags；
entry/SL/TP 不预填（留给 tpsl_advisor 建议按钮）。表头固定一句：
**「合流分是环境分不是下单信号，点位请自行确认」**（防 75 分=可以梭的误读）。
一期可降级为跳页+回退按钮。

**窄容器**（按 container 宽度非 viewport）：<768px 折叠态只留方向箭头+分数；
展开态改底部抽屉（占图高 50%）；触屏命中区 ≥44px；条目名 ≤6 字。

**z-index 共存**：HUD z-10 < TrapReasonCard z-20（点击型优先）< 工具栏弹层 z-50。

**同屏裁决口径收敛**（r1，本方案最重要的 UI 决策）：同屏原有三套裁决
（ReversalScorePanel 四条件打分环 / SupplyDemandCard 主力底牌 / 新增合流分）。
处理：ReversalScorePanel **收编**进 HUD 展开态微观确认组（其四条件本就是
合流分的反转四条件证据），HUD 上线后图下该面板默认收起（保留开关回退）；
SupplyDemandCard 保留（叙事型非打分型，口径不冲突）。

---

## 七、分歧点裁决结果（主控已裁，2026-08-13 定稿）

| # | 议题 | 主控裁决 | 依据 |
| --- | --- | --- | --- |
| D1 | HUD 默认开关 | **默认开**（折叠态 28px），可关可持久化 | 用户点名主打；「新层默认关」惯例适用于会洗图的叠加层，不适用于此 |
| D2 | ReversalScorePanel 归属 | **收编进 HUD 展开态微观确认组**，迁移时四条件语义不变 | 同屏三套裁决口径必然把用户搞晕（当日用户反复抱怨的正是这类问题） |
| D3 | 行为徽章呈现 | **只进行动闸口**：预填按钮禁用态+倒计时 tooltip，折叠态最多一枚🔒 | 盘面常驻连亏计数是焦虑制造机，违背情绪风控初衷 |
| D4 | 演示/mock 数据 | **一律按 unavailable，绝不进分** | 与全系统诚实化改造同一原则——分数是决策用的 |

---

## 八、实施拆单与工作量估算（v2 修正）

| 单 | 内容 | 负责 | 估算 |
| --- | --- | --- | --- |
| C-1 | jarvis_confluence.py（聚合+评分+预登记+降级语义）+ 冒烟 | 后端（agent-8） | 1 天 |
| C-2 | /api/confluence 端点 + 缓存接线（dashboard 区段） | 后端（agent-8） | 0.5 天 |
| C-3 | K 线页 HUD 组件（折叠/展开/防噪/窄容器/z-index/图标复用）+ ReversalScorePanel 收编 | 前端（agent-9/6） | 2–2.5 天（主控口径） |
| C-4 | 联调 + 零出网基线对照 + 文档 | 双方 | 0.5 天 |

**合计 ≈ 4–4.5 人日**（后端 1.5 + 前端 2–2.5 + 联调 0.5）。

**可砍项（供用户选，降为二期）**：

| 可砍项 | 一期替代 | 节省 |
| --- | --- | --- |
| 展开态行点击联动点亮叠加层 | 展开态只显状态文字，不联动图层 | ≈0.25 天 |
| Chart 页内抽屉复用 PlanForm | 跳导师页 + 回退按钮（预填参数经 URL 带过去） | ≈0.5 天 |

---

## 九、里程碑验收口径

1. HUD 分数与导师裁决在同一证据快照下方向一致（分数语义不同但方向不打架）。
2. 封禁期打开 K 线页：HUD 各 skipped 条目灰显、分数灰显，无假数据。
3. 点击 HUD →导师表单预填正确（方向/TF/entry=现价/SL-TP=advisor 分布位）。
4. 全链零新增出网（对照 jarvis_net 权重计费前后基线）。
