# 贾维斯 13 诊断驱动 · 待开发交接清单

> 生成时间：2026-08-06 13:30（UTC+8）· 生成者：主控 MCP-12（BajieAsk-agent-12-egvph0gj）
> 用途：窗口不稳定随时会掉，本文档记录「已完成 / 在途 / 未开发」全量状态，下次开新窗口可直接按本文档接续开发。
> 主计划契约见：`Vibe-Trading/贾维斯-13诊断驱动-策略进化开发计划.md`（D0–D7 每个任务有精确文件/函数/行号/验收）

## 一、写文档时刻的仓库状态

- 仓库位置：`Vibe-Trading/`（工作区根不是 git 仓库）
- HEAD = `78df70c`（D3），工作区干净，全部未 push
- 生产 7899 上次重启加载到 `4530b55`（D0）+`a06cb7b`（D1）——**D2/D3 及以后代码还没进生产，B 路全部完成后需再重启一次**

## 二、已完成 ✅（勿重复开发，接手前先 `git log --oneline` 核对）

| 项 | commit | 说明 |
| --- | --- | --- |
| S1–S7 止血门禁链 | 6a2b7ac / 8615d9c / bfd0d73 / e52fc08 / d8d4f87 / 7bff6f5 | 止损杠杆解耦、期望值门禁、熔断、5m 严门禁、逆势过滤、资金费率模拟 |
| S6 归因接口 | d76f5e5 | GET /api/twelve/attribution |
| D0 环境快照落库 | 4530b55（+前置 79b558a） | 双表 +11 ctx 列，TTL 缓存，容错落 NULL |
| D1 归因环境切片 | a06cb7b | ?dim=regime\|vol\|wyckoff\|direction\|hour + stable_losers |
| D2 量能/CVD 确认层 | 8ebb378 | suspect 打标降权 0.5 不拒单，_apply_context 公共设施 |
| D3 regime/威科夫趋势层 | 78df70c | S5 拒单改 mode 开关默认 deweight |
| 启动脚本误删修复 | （git checkout 恢复） | start.sh/stop.sh，restart-all.sh 可用 |
| 深度阶梯根因排查 | 只读无 commit | 现货 WS 簿 vs 合约 K 线口径不一致，基差约 1U |

## 三、在途 🟡（掉线时最可能中断的部分，接手先核实）

派发时间 2026-08-06 13:25 前后，各任务 result 应回给主控 MCP-12；若该窗口已掉，直接按下面的验收方法自查。

| # | 任务 | 派给 | 核实方法 |
| --- | --- | --- | --- |
| 1 | B 路 D4（funding/OI 拥挤度降权 0.6）→ D5（ATR 自适应止损 SL≥1.5×ATR，可硬拒 sl_below_atr）→ D6（S3 熔断+S4 tf_gate 改 deweight 模式，熔断降权 0.25） | MCP-3 | `git log --oneline` 找「13诊断D4/D5/D6」；缺哪棒补哪棒，契约在主计划文档 |
| 2 | D7 反向信号影子验证：新建独立脚本 `jarvis_twelve_shadow.py` + 独立表（如 twelve_shadow_trade），基于 stable_losers 的 system×tf×ctx_regime 组合、env 匹配才开反向单；样本不足时优雅空转 | MCP-5 | 看 `jarvis_twelve_shadow.py` 是否存在且有 commit；约束：禁碰 trader/config/smoke，参数暂用模块常量+TODO |
| 3 | 深度阶梯修复方案 A：WS 簿为 spot 且行情为合约口径时，/api/orderbook/live 改返合约 REST 快照（fapi /fapi/v1/depth，0.5 桶聚合，响应 JSON 结构不变+source 字段，TTL 2-3s 限频，REST 失败回退现货） | MCP-8 | `curl :7899/api/orderbook/live` 看 source 字段及 mid 是否与合约顶栏价一致 |
| 4 | D0 的 11 个 ctx 列镜像到若依 MySQL（jiaweisi 库 jarvis_sim_trade/position，改 jarvis_sync_tasks_sim.py 列映射 + MySQL 幂等 ALTER；无 ALTER 权限则出 sql/ 下 DDL 文件待手动执行） | MCP-13 | MySQL 侧 `SHOW COLUMNS FROM jarvis_sim_trade LIKE 'ctx_%'` |
| 5 | 若依同步 GRANT DELETE 授权修复（tape_bar/market_snapshot 自 8/05 报 1142 DELETE denied） | MCP-4（显示已完成但结论未追回） | 看 sync.log 是否还报 1142、market_snapshot 游标是否前进；没修则手动 root 执行 `sql/jarvis_mysql_watchlist_cleanup.sql` 里的 GRANT DELETE |

## 四、未开发 ⏸（本轮未派发，按顺序做）

1. **B 路完成后重启生产**：`./restart-all.sh`，验证四进程（daemon/sync/twelvesim/dashboard）+ `curl :7899/api/twelve/attribution?days=7&dim=regime` 出真实 env 数据
2. **D7 参数迁入 jarvis_config**：D2–D6 合入后把 shadow 脚本的模块常量迁到 jarvis_config（DEFAULTS+GROUPS+BOUNDS 三处登记）
3. **深度阶梯根治方案 C**：代理放行 `fstream.binance.com`（需用户手动改代理配置），恢复后合约 WS 直连，方案 A 自动退位
4. **数据积累期（时间任务，非代码）**：D0 上线起积累 ≥7 天环境样本 → 用 D1 报表（?dim=regime/vol/wyckoff）看「哪个信号在哪种环境亏」→ 对症补下一批指标
5. **D7 影子验证收获期**：样本齐后核对 stable_losers 命中与反向影子单表现
6. **阶段 2 参数自调优**：前提是 S6/D1 报表看到周净利转正，此前禁止上自学习（负期望时自学习是亏损放大器）

## 五、接手须知（硬约束）

- **文件独占**：`jarvis_twelve_trader.py` / `jarvis_config.py` / `_twelve_sim_smoketest.py` 三文件由 B 路串行链独占，D4–D6 没做完前其它任务禁改这三个文件，否则合并冲突
- **保留调用点**：改 `_do_close` 时保留 S7 资金费计提（非爆仓分支）和 S3 熔断簿记（尾部）；改 `_pre_gate` 时保留 S5 _trend_context 接线
- **验收标准**：每棒独立 commit 不 push；`_twelve_sim_smoketest.py` 全绿 + `_wyckoff_smoketest.py` 48/48 + `_supply_demand_smoketest.py` 27/27 零回归 + py_compile
- **设计理念**：诊断驱动——不关信号、不拒单（D5 风控例外），只打标签+降权，保证样本不断，让数据说出「缺什么指标」
- **口径**：诊断一律用 twelve_sim 实际成交口径（twelve_sim_trade），不要用信号边沿回测口径

## 六、下次开窗快速恢复流程

1. `cd Vibe-Trading && git log --oneline -15` 对照第二、三节确认实际进度
2. 逐项跑第三节「核实方法」确认在途任务死活
3. 缺哪个做哪个：契约细节全部在《贾维斯-13诊断驱动-策略进化开发计划.md》对应任务节
4. 全部代码完成后执行第四节第 1 步重启生产，然后进入数据积累期
