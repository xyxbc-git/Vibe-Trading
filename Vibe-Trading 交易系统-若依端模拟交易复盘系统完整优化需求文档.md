# Vibe\-Trading 交易系统\-若依端模拟交易复盘系统完整优化需求文档

## 一、项目整体说明

项目名称：**@Vibe\-Trading**

部署路径：本地若依项目 /Users/jolly/MyselfProject/JiaWeisiRuoyi/RuoYi\-Vue

模型要求：固定使用 fable5 模型

核心原则：**完全解耦原有12指标业务、不改动原有信号逻辑、不影响系统性能**，仅做数据同步、独立业务处理、模拟交易、数据复盘分析。

## 二、现有系统现状

- 后端信号模块由 Python 开发，内置 **12套交易信号策略**：道氏、艾略特波浪、缠论、123法则、摆动震荡、三重平滑RSI 等；

- 信号覆盖6个时间周期：**5m、15m、30m、1h、4h、1d**，每个周期独立生成入场价、止盈价、止损价、推荐杠杆、仓位比例；

- 信号支持**实时动态更新、点位变更**，当前仅有信号输出，无变更记录、无模拟交易、无盈亏统计；

- 原有12指标功能完全独立运行，本次新增功能**只读同步数据**，不侵入原有逻辑、不占用原有性能；

- 若依系统为核心数据存储与可视化后台，本次所有模拟交易、日志、统计数据全部落地若依数据库。

## 三、新增核心功能：若依端【模拟交易器】模块

### 1\. 系统配置项（yml数据库新增配置）

在若依配置文件/数据库中新增 **模拟交易器** 独立配置项，支持手动配置交易标的：

- 示例：配置为 **ETH** 后，系统自动监听 ETH 全周期12指标信号；

- 支持后续扩展其他币种，配置即生效、无需改代码；

- 配置独立隔离，不影响原有任何交易信号配置。

### 2\. 多周期分组独立模拟交易（核心业务）

对 5m/15m/30m/1h/4h/1d **6个时间轴分组隔离**，每个周期、每个信号策略独立跑模拟交易，互不干扰。

每组独立配置参数（支持自定义\+系统默认兜底）：

- 初始本金：默认 100 本金/单组，支持后台自定义修改；

- 交易杠杆：支持分组独立配置，无配置则沿用Python信号推荐杠杆；

- 仓位比例：按信号推荐本金百分比开仓，支持自定义约束；

- 止盈止损：优先读取实时信号点位，信号更新则自动更新持仓风控点位，并记录变更日志。

### 3\. 全自动模拟交易逻辑（完整闭环）

整体逻辑：Python实时信号同步至若依 → 若依自动生成模拟订单 → 接入ETH实时行情盯盘 → 自动平仓判盈亏 → 全量记录归档。

1. **模拟开仓触发**：任意周期、任意策略产生新入场信号，若依自动创建模拟持仓订单，绑定：周期、策略名称、开仓价、止盈价、止损价、杠杆、本金、开仓时间；

2. **实时行情盯盘**：持续接收ETH最新价格，实时校验持仓订单；

3. **自动平仓判结果**

    - 价格触碰止盈点位 → 自动止盈平仓 → 判定为盈利（胜）；

    - 价格触碰止损点位 → 自动止损平仓 → 判定为亏损（败）；

    - 持仓期间信号点位更新，自动更新订单止盈止损，留存变更记录，延续盯盘逻辑；

4. **订单闭环归档**：平仓后永久归档，不可篡改，用于长期数据分析。

### 4\. 数据同步原则（性能无损）

- 采用**数据只读同步**模式，只拉取/接收原有12指标的输出结果，**不修改、不阻塞、不占用原有指标计算性能**；

- 原有信号展示、推送、业务逻辑完全原样运行，新增模拟交易为独立旁路业务；

- 所有计算、存储、日志均在若依端独立执行，实现业务解耦。

## 四、数据库表设计（需新增数据表）

在若依数据库新增三张核心业务表，字段完整覆盖复盘、统计、追溯需求。

#### 1\. 模拟交易配置表（sim\_trade\_config）

用于存储模拟交易全局/分组自定义配置

- 交易标的（ETH/其他币种）

- 时间周期（5m/15m/30m/1h/4h/1d）

- 策略名称（道氏/波浪/缠论/123法则等）

- 默认本金、自定义本金

- 默认杠杆、自定义杠杆

- 默认仓位比例

- 配置状态、更新时间

#### 2\. 信号点位变更日志表（sim\_signal\_log）

记录所有信号点位变动，用于追溯复盘

- 周期、策略名称、交易标的

- 更新前：入场价、止盈价、止损价

- 更新后：入场价、止盈价、止损价

- 信号更新时间戳

- 关联模拟订单ID（如有持仓）

#### 3\. 模拟交易成交记录表（sim\_trade\_record）

每一笔模拟交易完整明细，核心统计数据源

- 订单ID、交易标的、周期、策略名称

- 开仓时间、平仓时间、持仓时长

- 开仓价格、最终平仓价格

- 开仓止盈/止损、最终生效止盈/止损（变更后）

- 初始本金、剩余本金

- 单笔盈亏金额、盈亏比

- 交易结果（止盈胜利/止损失败）

- 杠杆倍数、仓位占比

- 订单状态（持仓中/已平仓）

#### 4\. 周期策略统计汇总表（sim\_trade\_stat）

自动聚合统计，用于前台可视化展示

- 周期、策略名称

- 总交易次数、盈利次数、亏损次数

- 胜率、失败率、容错率

- 平均盈亏比、累计收益、最大回撤

- 统计时间区间

## 五、若依前端UI页面设计要求（小白友好、交易级清晰）

独立新增【模拟交易复盘系统】专属页面，沿用若依Vue原生布局规范，**核心强化币种强隔离、精准过滤能力**，严格落地「选什么币种、页面只展示该币种全部数据」的核心要求，彻底杜绝多币种数据混杂、统计错乱、冗余数据干扰问题。页面整体结构为：币种置顶筛选区 \+ 细分筛选区 \+ 币种专属统计看板 \+ 币种专属明细表格 \+ 分页，数据层级极简清晰，小白可零学习成本看懂对应币种完整交易复盘数据。

### 1\. 页面模块划分

- **币种专属筛选\+全局配置面板**：页面置顶固定核心币种下拉选择框，默认自动读取若依yml配置的默认模拟交易币种；用户选中ETH/其他币种后，页面**全局强制过滤、全模块刷新隔离**，系统后台查询SQL自动拼接币种筛选条件，前端所有模块仅加载、展示当前选中币种的配置数据、统计数据、持仓订单、交易记录、信号变更日志，彻底屏蔽所有非当前币种冗余数据。支持手动切换任意币种，切换瞬间全页面数据实时刷新适配，无残留、无混显。同时支持查看/修改当前选中币种，各周期、各策略的独立本金、杠杆默认配置。

- **币种专属数据统计看板**：卡片式**仅展示当前选中币种**下，各周期、各策略的胜率、盈亏比、总收益、交易次数，统计数据完全隔离，无跨币种数据干扰，数据精准匹配所选标的。

- **币种专属实时持仓列表**：仅展示当前选中币种的模拟持仓订单、实时浮动盈亏、剩余本金，过滤所有非当前币种持仓数据，页面数据纯粹无冗余。

- **币种专属历史交易明细**：仅展示当前选中币种的所有平仓订单完整记录，支持按周期、策略、时间、盈亏二次筛选，所有明细数据严格绑定当前所选币种。

- **币种专属信号变更日志**：仅展示当前选中币种的点位修改流水记录，精准追溯对应币种的信号变动，无无关币种日志干扰。

### 2\. UI交互规范

- 盈亏颜色区分：盈利绿色、亏损红色、持仓中性灰色，直观易懂；

- 支持表格列自定义显隐、排序、筛选、导出数据；

- 关键数据（胜率、盈亏比、剩余本金）加粗高亮展示；

- 所有时间、价格、金额字段格式化展示，杜绝原始数据晦涩格式；

- 无冗余装饰，高信息密度，兼顾专业度与小白可读性；核心严格执行**币种强隔离、单币种数据纯净展示**原则，从后端SQL查询、接口数据返回、前端页面渲染全链路过滤，确保查看单一币种数据时，统计结果、订单明细、日志记录100%精准匹配，无任何跨币种数据干扰，页面干净清晰、一目了然。

## 六、核心业务目标

1. 完全解耦原有12指标体系，**零性能影响、零功能冲突**；

2. 全周期、全策略独立模拟交易，完整记录信号变更与交易闭环数据；

3. 自动统计各时间周期、各指标策略的**胜率、失败率、容错率、盈亏比**；

4. 通过长期量化数据，精准筛选出表现最优的周期与指标组合，为实盘策略优化提供数据支撑；

5. 后台数据可视化清晰直观，研发、运营、新手均可快速看懂复盘结果。

## 七、开发约束与交付要求

- 固定使用 fable5 模型开发

- 所有新增代码、页面、数据表独立隔离，不改动原有业务代码；

- 数据同步为旁路监听，不阻塞原有信号更新逻辑；

- 所有字段可追溯、数据可导出、统计可自动刷新。


