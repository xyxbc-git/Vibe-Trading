# 13 诊断驱动 · 策略进化 — 开发计划

> 版本：v1.0 | 日期：2026-08-06 | 起草：全栈工程师会话（读码取证 + 契约固化）
>
> **理念（用户定死，不可改）**：12 信号模拟盘是【策略诊断实验场】，不怕亏，
> 亏损 = 暴露问题的数据。**不关闭任何信号**，只给信号装『市场环境上下文的
> 眼睛』+ 按环境**打标签 / 降权**，让它在对的环境发、错的环境降权；并识别
> 『某环境稳定亏』的信号组合——稳定亏 = 可反向利用的 alpha。
>
> **硬门禁豁免范围**：仅 D5 止损结构与旧 S1/S2（止损距离 / 费用期望）这类
> 纯风控允许拒单；其余一切信号侧判断一律『标签 + 降权 + 继续跑』。
>
> **面向执行工程师**：本计划假设你对仓库零上下文。每个任务给出精确文件
> 路径、函数名、行号（以 2026-08-06 工作区为准，行号会随并行提交漂移，
> 以函数名为锚）、调用契约、验收命令。每任务独立 commit，不 push。

---

## 〇、当前进度（断电后从这里接着做）

> 规则：每完成一项，把对应任务的 ⬜ 改成 ✅，并在「最近一次改动」补一行摘要。

- **当前阶段**：计划起草完成，待派发
- **下一步要做的任务**：D0（阻塞后续所有诊断任务）
- **已完成任务数**：0 / 8
- **最近一次改动**：2026-08-06 D1 完成——`api_twelve_attribution` 扩 `?dim=regime|vol|wyckoff|direction|hour`（单元格增 env；ctx 列不存在/全 NULL 归 unknown+notes）+ `stable_losers`（样本≥`twelve_diag_min_samples` 且胜率<`twelve_diag_max_winrate` 且净亏，flip_hint 理论口径）；两阈值进 jarvis_config（DEFAULTS+GROUPS(signal)+BOUNDS）；缓存 key 拼 dim；无 dim 与 S6 同构零回归。注入测试 25/20% 命中、19 笔被 min_samples 拦；direction 维度与手工 SQL 对账一致（D0 未合入，regime 全 unknown+note 待 D0 数据补验）

---

## 一、基线取证（2026-08-06 读码结论 · 执行前必读）

### 1.1 亏损止血 S1-S5 的真实状态（与旧计划文档不符，以代码为准）

旧计划《贾维斯-12信号亏损优化-开发计划.md》进度节写「S6 完成、下一步 S1」，
**实际代码已全部落地**：

| 旧任务 | 状态 | 证据 |
| --- | --- | --- |
| S1 止损门禁+杠杆解耦 | ✅ 已 commit `6a2b7ac` | `jarvis_twelve_trader.py` `_risk_gate`（约 L785）、`_auto_leverage`（约 L767）、`MIN_SL_PCT_BY_TF`（约 L146） |
| S2 费用期望门禁 | ✅ 已 commit `8615d9c` | `_risk_gate` 内 `rr_too_low` / `fee_negative_ev` 分支（约 L801-808） |
| S3 战绩熔断器 | ✅ 已 commit `bfd0d73` | `_breaker_gate`（约 L1281）、`_breaker_on_close`（约 L1312）、新表 `twelve_sim_breaker` |
| S4 周期门禁 | ✅ 已 commit `e52fc08` | `_pre_gate` 内 `tf_gate` 分支（约 L1415-1420） |
| S5 逆势过滤 | ✅ 已实现（工作区未提交） | `_trend_context`（约 L1391）、`_pre_gate` 内 `counter_trend` 分支（约 L1421-1429） |
| S6 归因报表 | ✅ 已 commit `d76f5e5` | `jarvis_dashboard.py` `api_twelve_attribution`（约 L5075-5174） |

**含义**：本计划的 D3/D6 不是新造，而是**对已上线拒单门禁的模式改造**
（reject → deweight），必须保留 reject 模式做零回归回退开关。

### 1.2 关键契约速查（读码核实过的真实签名）

- **拒单留痕**：`_do_reject(conn, sym, tf, system, direction, reason, entry, sl, tp, price, now)`（约 L1160）落
  `twelve_sim_position status='rejected'` + `twelve_sim_signal_log change_kinds='reject'`；
  `_reject_plan(conn, pen, reason, price, now)`（约 L1208）处理 pending 成交时刻拦截。
- **门禁链序**：`_pre_gate(conn, sym, tf, system, direction, strength, now)`
  （约 L1409）：S4 tf_gate → S5 counter_trend → S3 `_breaker_gate`；
  参数级 `_risk_gate(tf, entry, params)`（约 L785）：S1 → S2。
- **开仓落库**：`_do_open(conn, sym, tf, system, direction, price, balance, params, now)`
  （约 L875）与 `_fill_plan(conn, pen, plan, eff, balance, price, now)`（约 L1476）
  ——**D0 快照必须同时挂这两个入口**（立即成交 + 挂单触达成交）。
- **regime**：`jarvis_regime_classifier.classify(symbol, klines_15m=None, klines_1h=None, klines_4h=None, fetch_missing=True) -> RegimeResult`
  （L442），`RegimeResult` 字段 `regime("trending"|"ranging"|"breakout") / confidence / direction / strength / sub_regime / reasoning / indicators`（L37-48）。
  ⚠️ **无进程内缓存**，每次调用拉 3×200 根 K 线（`_fetch_klines` L415）。
- **regime 落库先例**：`jarvis_paper_trader.py` 已有完整同款实现——
  `signal_regime` 列（L114-120 列注册表 + ALTER TABLE 循环 L161）、
  `_classify_regime(symbol)` + `_REGIME_CACHE` TTL 缓存（L572-593）。**D0 直接镜像该模式**。
- **wyckoff**：`jarvis_wyckoff.analyze(symbol, interval="1h") -> {ok, state:{side:"acc|dist|trend|unknown", phase:"A..E", confidence}, ...}`
  （L794），内置「最后一根已收盘 bar open_time 指纹」进程内缓存——trader 已经
  在 `_trend_context` 复用，D0/D3 继续复用，零新增出网。
- **CVD/量能**：`jarvis_delta_flow.fetch_bars(symbol, timeframe, limit)`（L80，
  TTL 缓存 + 防限频三道闸，只回已收盘 bar）；`compute_delta_cvd(bars) -> list[dict]`
  （L134，逐 bar 加 delta/cvd）；`jarvis_supply_demand.breakout_check(bars, cvd, trap, whale) -> {active, direction, verdict:"confirmed|suspect|unknown", reasons}`
  （L246）——**trap/whale 允许传 None**（函数已容错，L307/L314 判空）。
- **funding/OI**：`jarvis_market_intel.get_intel() -> {funding:{rates:{SYM:率}}, oi:{symbol,value,change_pct}, long_short:{...}, fng:{...}}`
  （L211，四源并行 + 独立 TTL 缓存 + 过期后台刷新不阻塞）。
- **配置中心**：`jarvis_config.py` `DEFAULTS`（L60）+ `GROUPS`（L237）+
  `BOUNDS`（L356）+ `ENUMS`（L429）；dict 型键支持按 TF 分层（`_coerce` L467
  JSON 解析）；YAML 热加载（`load()` mtime 缓存 L536）。
- **冒烟**：`_twelve_sim_smoketest.py`——临时目录建库（`jsh.DB_PATH = jtt.DB_PATH`）、
  `jtt.latest_price` 直接打桩、`set_signal(tf, system, direction, plan, strength)`
  注入 `twelve_signal_state`、`check(name, cond, extra)` 断言器。上下文
  provider 同样用「模块属性打桩」手法（如 `jtt._market_context = lambda ...`）。

### 1.3 读码发现的与任务骨架不符之处（已按事实修正进契约）

1. **S3/S5 已上线**（骨架当它们是计划态）：D6 改造对象是真实运行中的
   `_breaker_gate`；D3 同时要改造 S5 的 `counter_trend` 拒单。S4 `tf_gate`
   同属「信号侧关闭类」，按理念一并纳入 D6 的模式开关。
2. **regime_classifier 无缓存**：每轮 72 槽位若裸调 `classify()` 会放大出网
   3 请求/次。D0 必须做 trader 级 TTL 缓存（镜像 `jarvis_paper_trader._REGIME_CACHE`）。
3. **market_intel 的 OI/多空比只有 BTCUSDT 口径**（`OI_SYMBOL = LS_SYMBOL = "BTCUSDT"`，
   L38-39）：ETH 槽位的 OI 特征只能作「BTC 全市场杠杆水位代理」，列名
   `ctx_oi_btc_chg` 明示口径；per-symbol OI 为可选扩展（D4b），默认不做。
4. **trader 是独立进程，无 WS 内存态**：`jarvis_supply_demand.collect_evidence`
   会去取 whale/book（WS 内存，dashboard 进程才有）→ 在 trader 进程恒空。
   D2 **不走** `collect_evidence`，直接 `fetch_bars + compute_delta_cvd + breakout_check(bars, cvd, None, None)` 三件套。
5. **共识单口径已有 regime 归因先例**：`/api/twelve/signal-stats`（dashboard
   约 L5043）已支持 regime 维度，但那是 `jarvis_paper_trader` 共识模拟单的表；
   本计划 D1 补的是**槽位引擎**（`twelve_sim_trade`）口径，两者并存不冲突。
6. **`twelve_sim_trade` 现无任何环境列**（DDL 约 L286-311 核实）；加列走
   `init_db` 现有 ALTER TABLE try/except 幂等模式（约 L275-283 先例，
   jarvis_db 兼容层对 pg 自动翻译 `ADD COLUMN IF NOT EXISTS`），无需独立
   migration 文件；SQL 草稿见 D0 附录。

---

## 二、任务总表

| 编号 | 状态 | 任务 | 文件 | 可并行 | 依赖 | 预估 |
| --- | --- | --- | --- | --- | --- | --- |
| D0 | ⬜ | 开仓时刻市场环境快照落库（基础） | trader + config | ❌ 阻塞一切 | 无 | 1d |
| D1 | ✅ | 归因报表按环境切片 + 稳定亏识别 | dashboard | ✅ 独立文件 | D0 字段（骨架可先行） | 1d |
| D2 | ⬜ | 量能/CVD 突破确认上下文层（标签+降权） | trader + config | ❌ 与 D3-D6 同文件串行 | D0 | 0.5d |
| D3 | ⬜ | 多周期趋势/regime 上下文层 + S5 改降权 | trader + config | ❌ 串行 | D0, D2 | 0.5d |
| D4 | ⬜ | funding/OI 拥挤度上下文层 | trader + config | ❌ 串行 | D0, D3 | 0.5d |
| D5 | ⬜ | ATR 自适应止损（纯风控，允许硬门禁） | trader + config | ❌ 串行 | D0, D4 | 0.5d |
| D6 | ⬜ | S3 熔断 / S4 周期门禁 → 标签+动态降权改造 | trader + config | ❌ 串行 | D0, D5 | 0.5d |
| D7 | ⬜ | 反向信号影子验证（可选） | 新独立脚本 | ✅ 独立文件 | D1 + ≥7 天样本 | 1d |

---

## 三、并行分发建议（多窗口派发用 · 必读）

**第一波（阻塞）**：只跑 **D0**。它改 `twelve_sim_trade`/`twelve_sim_position`
表结构与 `_do_open`/`_fill_plan` 两个开仓入口，是 D1-D7 一切诊断数据的来源；
D0 未 commit 前派发任何其它 trader 任务都会撞文件。

**第二波（两路并行）**：D0 commit 后——
- **A 路（独立窗口）**：D1。只改 `jarvis_dashboard.py`（只读查询 + 新响应字段），
  与 trader 零文件交集，可与 B 路完全并行。D0 未完成时也可先写骨架
  （环境列缺失时切片返回空 + note），但验收需 D0 数据。
- **B 路（单窗口串行）**：D2 → D3 → D4 → D5 → D6。**必须串行**，原因：
  五个任务全部修改 `jarvis_twelve_trader.py` 的同一批函数
  （`_resolve_entry_params` / `_pre_gate` / `_do_open` / `_fill_plan` /
  `_cycle_symbol`）与 `jarvis_config.py` 的 DEFAULTS/GROUPS/BOUNDS 三张表、
  `_twelve_sim_smoketest.py` 同一用例文件——并行必产生合并冲突。
  串行顺序按依赖排：D2 先建 `context_tags`/`size_factor` 公共设施，
  D3-D6 复用。

**第三波**：D7（新独立脚本 `jarvis_twelve_shadow.py`，可另开窗口），但业务上
依赖 D1 的 stable_losers 输出 + ≥7 天新口径样本积累，放最后。

---

## 四、逐任务契约

### D0 · 开仓时刻市场环境快照落库（P0 · 阻塞一切）

**文件**：`jarvis_twelve_trader.py`（`init_db`、`_do_open`、`_fill_plan`、
`_do_close`、新增 `_market_context`）、`jarvis_config.py`

**问题**：`twelve_sim_trade` 只有交易五要素（价格/时间/杠杆/费用/盈亏），
没有「开仓那一刻市场长什么样」——归因只能答『哪个信号亏』，答不了
『哪个信号在什么环境亏』。诊断实验场没有环境变量，等于实验没有对照组。

**改法**：
1. **加列**（`init_db` 的 ALTER TABLE try/except 幂等循环，同 L275-283 先例；
   `twelve_sim_position` 与 `twelve_sim_trade` 双表同列——快照在开仓时写
   position，平仓时由 `_do_close` 原样拷入 trade）：
   - `ctx_regime TEXT`（trending/ranging/breakout，`RegimeResult.regime`）
   - `ctx_regime_dir TEXT`（bullish/bearish/neutral）
   - `ctx_atr_pct REAL`（该 TF ATR/close×100，取自 regime indicators 或 K 线自算）
   - `ctx_vol_bucket TEXT`（low/mid/high：ATR% 历史分位 <30% / 30-70% / >70%）
   - `ctx_wyckoff TEXT`（`{side}-{phase}` 如 `acc-C`；trend/unknown 原样存 side）
   - `ctx_funding REAL`（该币最新 8h 资金费率，`get_intel().funding.rates[sym]`）
   - `ctx_oi_btc_chg REAL`（BTC OI 24h 变化%，`get_intel().oi.change_pct`，全市场杠杆水位代理——见 §1.3-3）
   - `ctx_hour_utc INTEGER`（`time.gmtime(entry_ts).tm_hour`，时段归因用）
   - `ctx_btc_trend TEXT`（BTCUSDT 1h regime 方向 bullish/bearish/neutral，带动过滤诊断用）
   - `context_tags TEXT`（D2-D4 上下文标签逗号串，D0 先建列占位）
   - `size_factor REAL`（D2-D6 降权系数乘积，D0 先建列默认 1.0）
2. **新增 `_market_context(sym: str, tf: str, now: float) -> dict`**：
   一次产出上述全部字段；内部三路取数全部带缓存——
   - regime / btc_trend：镜像 `jarvis_paper_trader._classify_regime`（L578）
     的 `_REGIME_CACHE` 手法，模块级 `{sym: (ts, RegimeResult)}`，TTL
     `twelve_ctx_cache_ttl_s`（默认 300s）；ETH 与 BTC 各缓存一份；
   - wyckoff：直接调既有 `_trend_context(sym)`（约 L1391，指纹缓存已内置）；
   - funding/OI：`jarvis_market_intel.get_intel()`（模块级 TTL 缓存已内置）。
   **失败语义**：任一路异常 → 对应字段落 `None`，**绝不阻塞开仓**
   （与 S5 `_trend_context` 同哲学：眼睛坏了不能把交易引擎卡死）。
3. **挂接**：`_do_open` 与 `_fill_plan` 开仓成功路径调 `_market_context`
   并写入 position 行；`_do_close` 平仓时把 ctx_* + context_tags + size_factor
   原样拷入 trade 行（SELECT position 行已有全量字段，随 INSERT 带过去）。
4. **配置**（DEFAULTS + GROUPS 双写；GROUPS 归 `signal` 组）：
   - `twelve_ctx_snapshot_enabled: True`（总开关，关=全落 NULL 零出网增量）
   - `twelve_ctx_cache_ttl_s: 300`（BOUNDS (30, 3600)）
5. **出网纪律**：不新增任何新出网端点；regime 的 K 线经
   `jarvis_regime_classifier._fetch_klines`（既有端点）+ 本任务 TTL 缓存限频
   （每币每 5 分钟至多 1 次 3×200 根）；funding/OI/wyckoff 全走既有缓存门面。

**验收**：
- smoke 新用例：打桩 `jtt._market_context = lambda s, t, n: {...固定 dict}` →
  开仓（立即成交与 pending 触达成交两条路径）后 position 行 ctx_* 齐全；
  平仓后 trade 行 ctx_* 与 position 一致；
- 打桩 `_market_context` 抛异常 → 开仓仍成功且 ctx_* 全 NULL；
- `twelve_ctx_snapshot_enabled=0` → 不调 provider；
- `python3 -m py_compile jarvis_twelve_trader.py` && `python3 _twelve_sim_smoketest.py` 全绿。

**SQL 草稿**（供 pg 手工核对；SQLite 由 init_db 自动执行）：

```sql
ALTER TABLE twelve_sim_position ADD COLUMN ctx_regime TEXT;
ALTER TABLE twelve_sim_position ADD COLUMN ctx_regime_dir TEXT;
ALTER TABLE twelve_sim_position ADD COLUMN ctx_atr_pct REAL;
ALTER TABLE twelve_sim_position ADD COLUMN ctx_vol_bucket TEXT;
ALTER TABLE twelve_sim_position ADD COLUMN ctx_wyckoff TEXT;
ALTER TABLE twelve_sim_position ADD COLUMN ctx_funding REAL;
ALTER TABLE twelve_sim_position ADD COLUMN ctx_oi_btc_chg REAL;
ALTER TABLE twelve_sim_position ADD COLUMN ctx_hour_utc INTEGER;
ALTER TABLE twelve_sim_position ADD COLUMN ctx_btc_trend TEXT;
ALTER TABLE twelve_sim_position ADD COLUMN context_tags TEXT;
ALTER TABLE twelve_sim_position ADD COLUMN size_factor REAL;
-- twelve_sim_trade 同款 11 列，此处从略
```

### D1 · 归因报表按环境切片 + 稳定亏识别（P0 · 可并行）

**文件**：`jarvis_dashboard.py`（扩展 `api_twelve_attribution`，约 L5075-5174）

**问题**：S6 报表只有 信号×TF×平仓原因 三维；没有环境维度就看不出
『oscillator 在 trending 环境亏、在 ranging 环境赚』这类可操作结论，
更找不出「稳定亏=反向 alpha」候选。

**改法**：
1. 扩 query 参数：`?days=7&dim=regime|vol|wyckoff|direction|hour`（可缺省）。
   `dim` 传入时聚合 SQL 的 GROUP BY 追加对应 ctx_ 列
   （regime→ctx_regime、vol→ctx_vol_bucket、wyckoff→ctx_wyckoff、
   direction→direction、hour→ctx_hour_utc），响应 `attribution` 单元格增
   `env` 字段；环境列全 NULL（D0 未上线或旧数据）时该维度归 `unknown` 桶并带 note。
2. 新响应块 `stable_losers`：对 `(system, tf, ctx_regime)` 组合
   （环境值非 NULL 才参与），过滤
   `trades ≥ twelve_diag_min_samples`（默认 20）**且**
   `win_rate_pct < twelve_diag_max_winrate`（默认 30.0）**且** `net_pnl < 0`，
   按 net_pnl 升序输出
   `[{system, tf, env, trades, win_rate_pct, net_pnl, flip_hint}]`——
   `flip_hint` = 反向后的理论胜率（100-win_rate）与理论净利（-net_pnl - 2×费用），
   作 D7 反向候选输入。诚实口径：flip 理论值未扣滑点、且反向后 SL/TP 结构
   不对称，仅作候选排序，不作收益承诺。
3. 两个诊断阈值走 `jarvis_config`（DEFAULTS+GROUPS `signal` 组 + BOUNDS）：
   `twelve_diag_min_samples: 20`（(5, 500)）、`twelve_diag_max_winrate: 30.0`（(0, 100)）。
4. 缓存沿用现有 `_cached(f"twelve:attr:{d}", 3600, _calc)` 手法，key 拼上 dim。

**验收**：
- `curl 'http://127.0.0.1:7899/api/twelve/attribution?days=7&dim=regime'`
  → ok:true 且单元格带 env；与手工 SQL
  `SELECT system,tf,ctx_regime,COUNT(*),SUM(pnl) FROM twelve_sim_trade ... GROUP BY 1,2,3` 对账一致；
- 注入 25 笔 ctx_regime='trending' 胜率 20% 净亏样本 → stable_losers 命中；
  样本 19 笔 → 不命中（min_samples 门槛生效）；
- 无 dim 参数时响应与 S6 现状字节级同构（零回归）。

### D2 · 量能/CVD 突破确认上下文层（P0 · B 路串行第 1 棒）

**文件**：`jarvis_twelve_trader.py`、`jarvis_config.py`

**问题**：突破/追价类信号（turtle/rule123/gap + dow/chanlun 的 market 入场）
只看价格破位；插针扫流动性与真突破在纯价格口径下不可区分——R10 取证
sl 平仓 56% 胜率 4.4% 的主力成分就是假突破追单。量能与主动单方向数据
（K 线自带 taker_buy）就在手边，没接。

**改法**：
1. 新增公共设施（D3-D6 复用）：
   - `_apply_context(params: dict, tag: str, factor: float) -> dict`：
     把 tag 追加进 `params['context_tags']`（逗号串去重）、
     `params['size_factor'] = params.get('size_factor', 1.0) × factor`；
   - `_do_open`/`_fill_plan` 的 margin 计算改为
     `margin = balance × position_pct% × size_factor`（factor=1.0 时字节级零回归），
     并把 context_tags/size_factor 落 position 行（D0 已建列）；
   - 每次打标落 `twelve_sim_signal_log`（`change_kinds='context'`、applied=1、
     note=中文原因），复用 `_breaker_log` 的写法（约 L1266）。
2. 新增 `_volume_context(sym: str, tf: str) -> dict | None`（TTL 缓存同
   `twelve_ctx_cache_ttl_s`）：
   `bars = jarvis_delta_flow.fetch_bars(sym, tf, 80)` →
   `rows = compute_delta_cvd(bars)` →
   `jarvis_supply_demand.breakout_check(bars, {"rows": rows}, None, None)`
   （trap/whale 传 None，函数已容错——见 §1.3-4）。返回
   `{verdict, direction, reasons}`；取数失败返回 None（放行不打标）。
3. 开仓/成交前（`_pre_gate` 之后、参数合成之后）：信号方向与突破方向同向且
   `verdict == 'suspect'` → `_apply_context(params, 'vol_suspect', twelve_ctx_deweight_suspect)`；
   `verdict == 'confirmed'` → 打标 `vol_confirmed`（factor 1.0，纯标记供归因）。
   **不拒单**。适用 system 集合可配 `twelve_ctx_vol_systems`
   （默认 `["turtle","rule123","gap","dow","chanlun"]`）。
4. 配置：`twelve_ctx_deweight_suspect: 0.5`（BOUNDS (0.05, 1.0)）、
   `twelve_ctx_vol_systems`（list 键）。

**验收**：smoke 打桩 `jtt._volume_context = lambda s, t: {"verdict":"suspect","direction":"up","reasons":[]}`
→ turtle 多单成交且 margin 为无标签基线的 0.5 倍、tags 含 `vol_suspect`、
signal_log 有 context 留痕；verdict=unknown → margin 不变无标签；
oscillator（不在 systems 集合）→ 不受影响。

### D3 · 多周期趋势/regime 上下文层 + S5 改降权（P0 · B 路第 2 棒）

**文件**：`jarvis_twelve_trader.py`、`jarvis_config.py`

**问题**：`SYSTEM_META` 自己写明 oscillator 是「震荡市利器、趋势市毒药」
（jarvis_twelve_systems.py 约 L894-899），但槽位开仓从不看当前是什么市——
就是缺这只眼睛。同时 S5 已实现为硬拒单（`counter_trend`），与『信号侧
保留继续跑』理念冲突，需改模式。

**改法**：
1. 复用 D0 的 regime 缓存（`_market_context` 内部缓存函数抽出为
   `_regime_of(sym)`）与 `_trend_context(sym)`：
   - regime=trending 且 system ∈ 均值回归集（默认 `["oscillator","triple_rsi"]`）
     且开仓方向逆 regime direction → `_apply_context(params, 'osc_in_trend', twelve_ctx_deweight_regime)`；
   - regime=ranging 且 system ∈ 突破集（同 D2 `twelve_ctx_vol_systems`）
     → 打标 `breakout_in_range` + 同系数降权；
   - 1h 威科夫逆势（S5 同判据：dist-C/D/E 拒多、acc-C/D/E 拒空，仅 5m/15m/30m）
     → 打标 `counter_trend` + `twelve_ctx_deweight_counter` 降权。
2. **S5 模式改造**：新配置 `twelve_trend_filter_mode`
   （ENUMS `("reject", "deweight")`，默认 `deweight`）。
   `_pre_gate`（约 L1421-1429）的 counter_trend 分支：mode=reject 保留旧
   拒单行为（零回归回退开关）；mode=deweight 时 `_pre_gate` 不再返回该
   reject_reason，改由本层打标降权。`twelve_trend_filter_enabled=False`
   仍为总关。
3. 配置：`twelve_ctx_deweight_regime: 0.5`、`twelve_ctx_deweight_counter: 0.5`
   （BOUNDS 同 (0.05, 1.0)）、`twelve_ctx_meanrev_systems` list 键、
   `twelve_trend_filter_mode` ENUMS。

**验收**：smoke 打桩 wyckoff dist-C + mode=deweight → 5m 多单**成交**且
margin×0.5 + tags 含 counter_trend；mode=reject → 拒单 `counter_trend`
（S5 旧用例全绿）；打桩 regime=trending/bearish → oscillator 多单降权，
turtle 顺势多单不降。

### D4 · funding/OI 拥挤度上下文层（P1 · B 路第 3 棒）

**文件**：`jarvis_twelve_trader.py`、`jarvis_config.py`

**问题**：R10 取证 short -123.2U（ETH 上行窗口全程逆势空）——挤在拥挤侧
的单最容易被 squeeze。funding/多空比是免费的拥挤度温度计
（`get_intel()` 已在拉），信号层为零消费。

**改法**：
1. `_market_context` 已取 funding（D0）；本层加判定：
   `|funding| ≥ twelve_ctx_funding_hot`（默认 0.0005 = 0.05%/8h）且开仓方向
   在拥挤侧（funding>0 开多 / funding<0 开空）→
   `_apply_context(params, 'crowded_side', twelve_ctx_deweight_crowded)`；
   反拥挤侧单打标 `contrarian_side`（factor 1.0 纯标记，归因看反向是否真优）。
2. BTC OI 24h 骤增（`ctx_oi_btc_chg ≥ twelve_ctx_oi_surge_pct`，默认 5.0%）
   且拥挤侧 → 标签升级为 `crowded_hot`（系数再乘一次 deweight_crowded）。
   口径注记：OI 为 BTC 全市场代理（§1.3-3），标签语义是「市场整体杠杆
   水位高」而非「该币 OI 高」。
3. funding 数据缺失（None）→ 不打标不降权（放行哲学）。
4. 配置：`twelve_ctx_funding_hot: 0.0005`（BOUNDS (0.00005, 0.01)）、
   `twelve_ctx_oi_surge_pct: 5.0`（(0.5, 100)）、
   `twelve_ctx_deweight_crowded: 0.6`（(0.05, 1.0)）。

**验收**：smoke 打桩 funding=+0.001 → 多单 margin×0.6 + `crowded_side`，
空单打 `contrarian_side` 不降权；funding=None → 无标签；
OI 骤增叠加 → `crowded_hot` 且系数 0.36。

### D5 · ATR 自适应止损（P1 · B 路第 4 棒 · 纯风控允许硬门禁）

**文件**：`jarvis_twelve_trader.py`（`_risk_gate`）、`jarvis_config.py`

**问题**：S1 的 `twelve_min_sl_pct` 是静态档（5m=0.5% 等）；波动率换挡后
静态档必然再次失配——低波动期 0.5% 太宽（漏拦不了噪声单），高波动期
0.5% 又太窄（正常止损也在噪声带内）。

**改法**：
1. `_risk_gate(tf, entry, params)` 增第四道校验（在 S1 静态档之后）：
   `sl_dist_pct < twelve_sl_atr_mult × atr_pct(tf)` → 拒单
   `reject_reason='sl_below_atr'`（新原因，进 `REJECT_REASON_CN` 与
   dashboard `_TWELVE_REJECT_REASONS` 枚举，约 L5071）。
   `atr_pct` 复用 D0 `_market_context` 的该 TF ATR 缓存；取不到 → 跳过本道
   （静态档兜底仍在，放行哲学）。
2. 这是止损结构纯风控（理念豁免区），**允许拒单**；但同样落
   `_do_reject` 留痕可复盘，S6/D1 报表自动纳入统计。
3. 配置：`twelve_sl_atr_mult: 1.5`（BOUNDS (0.3, 5.0)；海龟自身 2×ATR，
   1.5 为门槛下限而非建议值）。

**验收**：smoke 打桩 ATR%=0.5 的 5m 场景——SL 距离 0.6%（> 静态档 0.5 但
< 1.5×0.5=0.75）→ 拒 `sl_below_atr`；SL 0.8% → 放行；ATR 缓存打桩为 None
→ 0.6% 放行（回退静态档）。

### D6 · S3 熔断 / S4 周期门禁 → 标签+动态降权改造（P0 · B 路第 5 棒）

**文件**：`jarvis_twelve_trader.py`（`_pre_gate`、`_breaker_gate`、
`_do_open`/`_fill_plan`）、`jarvis_config.py`

**问题**：S3 熔断命中后「只推信号不开仓」= 该组合停止产生数据——诊断
实验场最怕的就是样本断流；稳定亏组合恰恰是最有反向价值的组合，关掉它
等于把 alpha 候选踢出实验。S4 tf_gate 同理（5m 低置信直接拒 = 5m 低置信
段永远没有战绩数据）。

**改法**：
1. 新配置 `twelve_cb_mode` 与 `twelve_tf_gate_mode`
   （ENUMS `("reject", "deweight")`，**默认均 `deweight`**）。
2. `_pre_gate` 改造：
   - S4 分支：mode=deweight 时不返回 `tf_gate`，改打标
     `tf_lowconf`（TF 停用配置 `twelve_tf_enabled=0` 例外——显式停用是
     运营指令，仍硬拒，语义不变）+ `twelve_tf_deweight`（默认 0.5）降权；
   - S3 分支：mode=deweight 时 `_breaker_gate` 命中 tripped/probing 不再
     返回 `circuit_breaker`，改打标 `breaker_deweight` +
     `twelve_cb_deweight`（默认 0.25）降权——**信号继续跑、数据继续攒**；
     `_breaker_on_close` 战绩簿记与 trip/recover 状态机**原样保留**
     （状态是诊断标签的事实来源）；半开试探机制在 deweight 模式下自动
     旁路（一直在跑，无需试探）。
3. `_pre_gate` 返回值扩展为 `(reject_reason|None, probe, tags: list[str])`，
   调用点（`_cycle_symbol` 内 3 处：pending 成交前、立即开仓前、挂计划前）
   把 tags 经 `_apply_context` 落进 params。**mode=reject 时行为与 S3/S4
   现状字节级一致**（零回归开关）。
4. 配置：`twelve_cb_mode`/`twelve_tf_gate_mode` ENUMS、
   `twelve_cb_deweight: 0.25`（BOUNDS (0.05, 1.0)）、
   `twelve_tf_deweight: 0.5`（同）。

**验收**：smoke——注入 30 连亏触发熔断后：mode=deweight 第 31 笔**成交**
且 margin×0.25 + tags 含 breaker_deweight，`twelve_sim_breaker.state`
仍为 tripped（簿记保留）；mode=reject 回退后 S3 旧用例全绿（拒单
`circuit_breaker`、冷却半开、试探赢恢复）；S4 同理双模式验证；
5m strength=0.5 + deweight → 成交且 `tf_lowconf` 降权。

### D7 · 反向信号影子验证（P2 · 可选 · 独立脚本）

**文件**：新建 `jarvis_twelve_shadow.py`（独立进程 CLI，绝不挂 dashboard）、
`_twelve_shadow_smoketest.py`

**问题**：D1 识别出的「某环境稳定亏」组合只是统计候选；反向是否真有
alpha 必须用影子单前向验证，不能拿历史 flip 理论值直接上。

**改法**：
1. 新表 `twelve_shadow_trade`（结构镜像 `twelve_sim_trade` + `src_system`/
   `src_env`/`flip_of_position_id` 列），独立钱包表 `twelve_shadow_wallet`
   ——**零接触**主台账五表。
2. `cycle` 逻辑：读 D1 `stable_losers`（直调 dashboard 归因函数或同 SQL），
   对命中组合订阅 `read_signals(sym)` 的对应槽位信号，**方向取反**开影子
   纸面单（SL/TP 镜像互换），生命周期复用 `_exit_check`/`_timeout_due`
   同款口径（import 复用 trader 纯函数，不复制粘贴）；
3. 环境匹配：仅当开仓时刻 `_market_context` 的 regime 与 stable_loser 的
   env 一致才开影子单（环境错配的反向没有统计依据）；
4. `report` 子命令：原向 vs 反向 净利/胜率/费用 对照表，样本 ≥
   `twelve_diag_min_samples` 才输出结论行。

**验收**：`python3 _twelve_shadow_smoketest.py` 全绿——注入稳定亏组合 +
同 env 信号 → 影子反向单开仓；env 不匹配不开；report 对照正确；
主台账五表零变更（行数前后一致断言）。

---

## 五、公共纪律（每任务必须遵守，写进每个 commit 的自查）

1. 所有开关/阈值默认值 `jarvis_config.py` **DEFAULTS + GROUPS 双写**
   （数值键补 BOUNDS、枚举键补 ENUMS），YAML 热加载即生效；
2. 上下文标签 / 降权原因**不静默丢弃**：全部落 `context_tags` +
   `size_factor` 列与 `twelve_sim_signal_log`（change_kinds='context'），
   D1 报表可复盘；拒单（仅 D5 与存量 S1/S2）走 `_do_reject` 留痕；
3. 每任务扩 `_twelve_sim_smoketest.py`（D7 独立 smoketest），打桩手法沿用
   模块属性替换（`jtt._market_context = ...`），全离线；
4. **不新增出网端点**：一切外部数据走 `fetch_bars` / `get_intel()` /
   `wyckoff.analyze` / `regime _fetch_klines` 既有渠道 + TTL 缓存限频；
5. 环境数据取不到 = 落 NULL / 不打标 / 放行，**绝不阻塞开仓主链路**；
6. 每任务独立 commit（信息格式对齐仓库现状 `feat: 13诊断D0——...`），不 push；
7. mode 开关（trend_filter/cb/tf_gate）reject 档 = 现状字节级行为，
   是零回归回退通道，任何改造不得破坏 reject 档旧用例。

---

## 六、验收与回归

- `python3 _twelve_sim_smoketest.py` 全绿（D0-D6 用例累积；含全部 mode=reject 回退用例）
- `python3 _twelve_shadow_smoketest.py` 全绿（D7）
- `python3 -m py_compile jarvis_twelve_trader.py jarvis_dashboard.py jarvis_config.py jarvis_twelve_shadow.py` 通过
- `_wyckoff_smoketest.py` / `_supply_demand_smoketest.py` 零回归
- `curl /api/twelve/attribution?days=7&dim=regime` ok:true 且与手工 SQL 对账一致
- **观察指标（上线 ≥7 天后用 D1 复盘）**：
  - 每笔 trade 的 ctx_* 非 NULL 率 ≥ 90%（快照链路健康度）；
  - `vol_suspect` / `counter_trend` / `crowded_side` 标签组的净利显著差于
    无标签组 → 降权方向正确（差异不显著则回调系数或撤标签）；
  - stable_losers 产出 ≥ 1 个样本充足候选 → 启动 D7；
  - 全场净利趋势与 sl 平仓占比对比 S1-S5 基线，数据说话再定第二轮。
