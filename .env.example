# 贾维斯驾驶舱配置（复制本文件为 .env 后填入你的 key）
# 用法：cp .env.example .env  然后编辑 .env，重启 jarvis_dashboard.py 生效
# 说明：已存在于系统环境变量里的同名变量不会被覆盖；不填则「问贾维斯」自动用规则问答兜底。

# ── DeepSeek（推荐，最省事，只填这一行即可）──
# 填上后 base 自动 = https://api.deepseek.com，model 自动 = deepseek-chat
DEEPSEEK_API_KEY=

# ── 可选：换模型（如用 R1 推理模型）──
# JARVIS_LLM_MODEL=deepseek-reasoner

# ── 可选：用 OpenAI 或其它 OpenAI 兼容中转，改成下面这组 ──
# JARVIS_LLM_API_KEY=sk-xxxx
# JARVIS_LLM_BASE_URL=https://api.openai.com/v1
# JARVIS_LLM_MODEL=gpt-4o-mini
