# 贾维斯「成交流画像」优化方案（盘口页 240min 窗口区块）

> 版本：v1.0 | 日期：2026-08-13 | 任务 BS3
> 主笔：agent-5（数据/算法派）× 对抗评审：agent-9（UX/消费者派，BS4 回传）
> 纪律：本文档只出方案不写代码；一轮对抗已收敛；取证全只读。
> 范围：盘口页（/depth）「成交流画像」区块——主力行为判定 / 主体份额分布条 /
> 指纹聚合表 / 实时成交流。代码：`jarvis_tape_classify.py`（分类与判定核心）、
> `jarvis_dashboard.py /api/tape/flow`（端点）、`desktop/src/pages/DepthView.tsx`（前端）。

---

## 一、现状口径取证（实读代码，行号为证）

| 项 | 现状口径 | 证据位置 |
| --- | --- | --- |
| 主体分桶 | 单笔金额三档：<$10k 散户 / $10k–100k 中户 / ≥$100k（`whale_tier1_usd`）机构；叠加 qty 精确签名指纹组（≥3 笔：买卖两侧笔数 min/max≥0.35→做市商；单侧且组均额≥$10k→机构拆单），指纹组结论优先于金额档 | `jarvis_tape_classify.py:100-129` |
| 主力行为判定 | inst+maker 合并净流；\|净流\|<$100k（tier1）恒显「中性」；否则按 净方向×窗口价格反应 判 砸盘/派发出货/吸筹/拉盘 四象限 | `:186-216` |
| 窗口 | 前端按主周期映射 window_min（1m→15 … 1h/4h/1d→240，后端硬上限 240）；**份额分解的数据源是 6000 笔环形缓冲**（`TRADES_MAX=6000`，注释自认「BTC 高频期约覆盖数分钟」）；240 个分钟桶仅用于 burst/fade/价格变化 | `:46-47,433-450`；`DepthView.tsx:84-95` |
| burst / 入场提示 | 近 2 分钟非散户额 vs 更早分钟中位数 ≥3× 且 ≥$100k → 脉冲警报；砸盘/派发中卖出额从峰值回落 ≤45% → 抛压减弱提示 | `:218-255` |
| 诚实基线 | `DISCLAIMER` 常量（「概率性画像，非投资建议」）随响应返回；WS 未就绪 active=false | `:36-37,443-445` |
| 展示 | VerdictCard 动作大字+非散户占比条；四主体分布条（份额%+净额）；指纹聚合表前 14；3s 轮询逐笔流 | `DepthView.tsx:442-516,1494-1498` |

**「机构 0.0% (+$60)」的成因定案**：窗口内 inst 桶只有极稀成交（净额 $60），
份额四舍五入为 0.0%，UI 照显精确数字、无样本量/置信度标注——**口径缺陷与
样本真稀双重叠加**：真稀是事实，把稀样本渲染成权威精确数才是缺陷。

**旁证**：T9/T10 实测采集覆盖率按 UTC 小时 30%–86%（笔记本作息+轮询采集），
断档期画像照常显示、无任何 gap 提示。

---

## 二、问题清单（按危害排序）

| # | 问题 | 危害 |
| --- | --- | --- |
| P1 | **窗口名不副实**：标称 240min，份额分解实际受 6000 笔环形缓冲限制，高活跃期只覆盖数分钟 | 用户以为在看 4 小时画像，实际是「最近 N 笔」——所有下游解读失真 |
| P2 | **身份标签过度承诺**：单笔大小≠身份（拆单算法普及后大单被拆成中/小单大小；零售也能打 $100k 单）。「机构 0.0%」在拆单时代系统性低估 | 权威感误导——小白把「机构 0.0% +$60」读成「机构在买」 |
| P3 | **样本稀时给精确数**：0.0%、+$60 无样本量/置信度门禁 | 把噪声渲染成信号 |
| P4 | **判定阈值一刀切**：净流阈值固定 $100k，不随窗口/活跃度自适应——高活跃期小净流被埋没、低活跃期永远「中性」 | 「中性」占绝大多数时间，信息量趋零 |
| P5 | **判定不接决策链路**：纯展示，不进导师证据链 | 小白看到大单→冲动市价单，与导师产品目标背道而驰 |
| P6 | **断档不可见**：WS 断线/采集断档期窗口数据缺失，画像无 gap 标注 | 断档期的份额/判定是残缺数据算出来的，用户无从知晓 |

---

## 三、最终优化项（双方合并，按优先级）

### P0（先诚实可读，合计约 2.5d）

**A1 诚实化三件套**（后端字段 + 前端渲染，~0.5d）
- 响应增 `actual_window_min`（trades 实际覆盖跨度）与 `coverage`——UI 主口径
  直接显「**近 Xmin 实测**」单一诚实数字（标称窗口退 tooltip，避免双数字认知负担）；
- coverage 翻译人话三档：连续 / 有断档 / 严重断档，配分钟桶缺口灰条（缺口>2min 记 gaps）；
- 样本量门禁：actor 桶 n<10 笔或总额过小 → 显「样本不足」不给精确百分比，
  **堆叠条色块与净额红绿方向色一并灰化**（不只灰数字）；verdict 总样本不足时
  强制「不可判定」。对齐 T8/T9「样本不足显示不可判定不给结论」的全项目纪律。

**A2 身份标签 → 单笔规模分布 + 置信度**（~1d）
- 主标签改「**单笔规模分布**」（大单 ≥$100k / 中单 $10k–100k / 小单 <$10k），
  身份词（机构/散户）降为副标签并口语化标注「按单笔金额推断，非真实身份」；
- 只有**指纹组**（有行为证据：等量拆单/双边报价）才配身份词，且带文字三档置信度：
  证据充分（组≥5 笔）/ 一般（3–4 笔）/ 仅金额分层（无组证据）——不用小数；
- `DISCLAIMER` 从响应字段升级为 UI 常驻脚注。

**A3 L0/L1/L2 三层信息架构**（agent-9 新增，UI 层几乎零算法成本，~0.5d）
- **L0 一句话结论**：【谁】在【做什么】+【置信度】+【对你意味着什么】
  （例「主力疑似吸筹（证据充分）——短线别追空，等回踩确认」）；样本不足时
  强制降级「样本不足，暂不判定——观望，别拿这个当依据」；
- **L1 ≤3 条人话证据**：复用导师裁决卡 pass/warn/fail 视觉语言（用户已有认知资产），
  判定依据显性化（「窗口净流 $X（阈值 $Y）」——A6 自适应后防玄学感）；
- **L2 原始数据**：指纹表+逐笔流默认折叠「看原始数据」。

**A4 逐笔成交流默认折叠**（agent-9 新增，~0.1d）
- 3s 滚动数字流对情绪化交易者是追单诱发器，与导师产品目标直接冲突——默认收进
  L2 折叠；burst 事件警报保留上浮。

### P1（再接进决策与增强判读，合计约 2d）

**B1 判定→决策接口**（原 O5，agent-9 主张升级，~0.5d）
- mentor `micro` 证据路加 `tape_verdict` 输入（action/nr_share/burst/entry_hint），
  与 seatbelt/sentiment 同构扩展，不新增权重项（预登记权重零改动）；
- L0 结论卡加「**问导师这单能不能做**」按钮跳导师页预填方向——把
  「看到大单→冲动市价单」改造成「看到信号→写计划→裁决」闭环；
- 盘上合流仪表（agent-8×agent-6 另组在建）按输出契约消费，见 §五边界声明。

**B2 双窗对比（30m 当下 vs 240m 背景）**（原 O4 + agent-9 修正，~0.5d）
- 双窗数据放 **L1 证据层**；L0 必须单一合成结论（「短线转买、背景仍卖」这类冲突
  由系统给结论句，不让用户自己裁判两个窗口）；反对 >2 窗（信息密度与轮询开销）。

**B3 结论翻转阻尼**（agent-9 新增，~0.5d）
- L0 结论连续 3 轮同向才切换文案；5 分钟内翻转 ≥3 次显「市场混沌，信号打架——观望」。
  防盯盘成瘾与「判定玄学感」。

**B4 判定阈值自适应**（原 O6，~0.5d）
- 净流阈值从固定 $100k 改 `max(窗口总额 × x%, $100k)`；判定依据进 L1（配合 A3）。

### P2（证据增强，约 1d，落地必须以人话证据呈现）

**C1 拆单识别增强**（原 O3，排序按 agent-9 意见后移）
- 现指纹只抓「等量拆单」；增强：时间聚类（同方向+相近单量+短间隔突发）抓 TWAP
  变体、iceberg 检测（同价位连续吃单重现）；
- 输出「拆单归并后等效大单流」，机构口径按归并前/后**两口径对照**展示；
- 落地形态必须是 L1 人话证据（例「疑似同一主体拆 14 笔买入合计 $310K」），
  不允许只加表格行——这是 A2 置信度体系的长期数据支撑。

---

## 四、四道必答题（合并立场）

1. **分类可信度**：「机构 0.0% +$60」= 口径缺陷（稀样本渲染成权威精确数、无置信度）
   叠加样本真稀（inst 桶确实只有 $60 净额）。诚实做法三合一：改名「单笔规模分布」
   不强行贴身份（A2）+ 样本不足不给数（A1）+ 指纹组才配身份词并带置信度（A2）。
   拆单识别聚类有价值但排 P2（C1）——先诚实，再增强。
2. **判定→决策连接**：接，且按 agent-9 意见作为产品主线（P1 首位）——mentor micro
   路同构扩展 + 「问导师」按钮闭环；合流仪表只声明输出契约不重复建设（§五）。
3. **时窗**：240min 单窗既不够也名不副实。先修真实性（A1 实测窗口），再 30m/240m
   双窗（B2，双窗进 L1、L0 合成单一结论）。双方一致反对多窗矩阵。
4. **诚实纪律**：实测窗口覆盖 + 样本量门禁 + 断档 gap 标注 + 置信度文字三档，
   全部进**响应字段**（不是只改 UI）；UI 侧配合灰化/灰条/降级文案。

---

## 五、边界声明（与另组风暴不越界）

- 画像 L0 结论应作为盘上合流仪表（agent-8×agent-6 在建）的**一路输入行**展示——
  「一处结论、一处引用」，两处独立给结论必打架；若证据冲突需显式「证据冲突」态。
  本方案只定义输出契约：`{action, action_cn, confidence, nr_share_pct,
  actual_window_min, coverage, burst, entry_hint, l0_text}`，消费方式归另组方案。
- mentor 接入沿用其证据路契约（available/unavailable 不计分母），tape 引擎
  未就绪时诚实降级。

## 六、工作量与分歧记录

- **工作量**：P0 ≈ 2.5d（A1 0.5 / A2 1 / A3 0.5 / A4 0.1）；P1 ≈ 2d；P2 ≈ 1d。
  P0 独立可交付，先行。
- **分歧点（一轮对抗后收敛）**：
  1. O3（拆单增强）vs O5（决策接口）优先级——主笔原排 O3 在前（算法派：证据强度
     优先），agent-9 主张对调（消费者价值链：可信可读→可用→证据更强，算法再强
     若不能被小白消费则收益为零）。**收敛：采纳 agent-9 排序**（B1 进 P1、C1 降 P2），
     主笔保留一条：C1 是 A2 置信度的长期数据支撑，P2 非「可不做」。
  2. 窗口双数字展示——主笔原方案「标称 240min·实际 Xmin」并列，agent-9 指出双数字
     对小白是认知负担。**收敛：主 UI 单显实测口径，标称退 tooltip**。
  3. 其余各项双方一致（含反对 >2 窗、诚实字段进响应层）。
