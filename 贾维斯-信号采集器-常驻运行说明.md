# 贾维斯 T10 信号采集器 · 常驻运行说明

> 结论先行：**进程常驻 = 持续落带**。`jarvis_signal_collector.py run` 只要活着，
> 信号记录与真 bar 分钟数据就与面板无关地持续入库。
> 但**任何常驻方案都治不了「机器会睡」**——macOS 合盖/休眠时进程被冻结，
> WS 断流、bar 缺口照旧产生（唤醒后信号侧可自动回补，tape 分钟 bar 无法回补）。
> 缺口要清零只有一条路：把采集器放到**常开机器（VPS）**上跑。VPS 采购/迁移是
> 用户决策项（与专用出口 IP 是同一笔投入，见开发计划 §T10）。

## ⚠ 历史包袱声明：本采集器取代 sync_reco_plan 的信号驱动副作用

《贾维斯-战绩断流诊断-20260813.md》主因 1 已实锤：此前多标的信号重算没有独立
调度器，靠 `jarvis_sync_tasks_b.py` 的 `sync_reco_plan` 每 180s 逐币拉
`/api/twelve/consensus` 的**副作用**驱动（一石二鸟）。2026-08-05 该任务因「若依
页面下线无消费者」被停（`jarvis_sync_tasks_b.py:277`），信号量 3001→403 条/天、
9 标的只剩 ETH——隐式耦合无人知晓，一停全断。

**本采集器上线后，这个副作用彻底不再需要**：信号生产由独立常驻进程按已收盘
bar 事件驱动，与镜像同步、面板轮询完全解耦。**不要**通过取消
`jarvis_sync_tasks_b.py:277` 注释来「恢复」旧驱动——那会把信号生产重新绑回
同步任务副作用，事故根因原样复活。

多标的覆盖口径：标的集优先级 = CLI `--symbols` > 配置 `sigcol_symbols`（逗号
分隔，与 watchlist 解耦）> `watchlist`。要恢复历史 9 标的
（ADA/SOL/DOGE/BNB/XRP/BTC/ETH/XAU/SNDK）的信号覆盖而不动面板订阅，设
`sigcol_symbols` 后重启采集器即可（9 标的 × 5 TF = 45 kline 流 + 9 aggTrade
流，远低于币安单连接 1024 流上限；队列/单工作线程实测余量充足）。

## 0. 它跑起来之后你会得到什么

| 产出 | 位置 | 说明 |
| --- | --- | --- |
| 信号变更流水 | `twelve_signal_changes`（jarvis_journal.db / pg） | 每根已收盘 bar 触发 12 系统计算，实质变化落一条；`ts`=bar 收盘时刻，`price`=bar 收盘价 |
| 信号当前态 | `twelve_signal_state` | 与 dashboard 写入者共享；采集器带新鲜度守卫不互相回退 |
| 采集台账 | `signal_collect_log` | 每根已处理 bar 一行（覆盖率报表数据源），保留 `sigcol_ledger_retention_days`（默认 45 天） |
| 真 bar 分钟数据 | `tape_minute_bars` | `sigcol_tape=true`（默认）时同进程挂 aggTrade→分钟聚合链 |
| 业务日志 | `~/.vibe-trading/jarvis_signal_collector.log` | 种子/回补/心跳/异常 |
| 状态心跳 | `~/.vibe-trading/jarvis_signal_collector_status.json` | 每 60s 原子刷新，监控接入点 |

覆盖率自检（验收口径：连续 7 天 ≥95%、5m 按 UTC 小时偏斜 <1.2×）：

```bash
.venv/bin/python jarvis_signal_collector.py coverage --days 7          # 人读报表
.venv/bin/python jarvis_signal_collector.py coverage --days 7 --json   # 机读
```

## 1. 手动前台跑（先验证再常驻）

```bash
cd /Users/jolly/MyselfProject/TwosZiyan/AILiangH/Vibe-Trading
.venv/bin/python jarvis_signal_collector.py run
# 预期日志：种子 N 根 → 心跳：事件/已采/记录/队列/WS ✓
# Ctrl-C 退出
```

币种跟随配置中心 `watchlist`，TF 集走 `sigcol_tfs`（默认 5m/15m/30m/1h/4h）；
临时覆写：`run --symbols BTCUSDT,ETHUSDT --tfs 5m,1h`。
改配置后**重启采集器进程**生效（配置在 start 时快照）。

## 2. macOS launchd 常驻（本机方案）

模板已备好：仓库根 `com.jarvis.collector.plist`（与 `com.jarvis.sync.plist` 同风格，
KeepAlive 自愈拉起；仓库路径变更时改其中三处绝对路径）。

```bash
cp com.jarvis.collector.plist ~/Library/LaunchAgents/
launchctl load ~/Library/LaunchAgents/com.jarvis.collector.plist   # 安装并启动
launchctl list | grep com.jarvis.collector                          # 验活
launchctl kickstart -k gui/$(id -u)/com.jarvis.collector            # 强制重启
launchctl unload ~/Library/LaunchAgents/com.jarvis.collector.plist # 停止卸载
```

**本机方案的天花板（实测根因，非猜测）**：launchd 只保证进程不死，不保证机器不睡。
合盖/闲置休眠 → 进程冻结 → WS 断流（诊断报告实锤：08-12 18:20 → 08-13 09:14
引擎停摆 15 小时）。唤醒后采集器自动重连并回补信号缺口（REST 拉已收盘 K 线
回放，上限 `sigcol_backfill_bars`=288 根/对），但 `tape_minute_bars` 的分钟成交
无法回补（aggTrade 逐笔即逝）。**防休眠必须显式配置**（插电时）：

```bash
sudo pmset -c sleep 0 disablesleep 1   # 插电永不睡（合盖也不睡，注意散热）
sudo pmset -c sleep 0                  # 温和版：插电不自动睡（合盖仍睡）
pmset -g | grep -E "sleep|disablesleep" # 核对当前生效值
```

**开机自启边界**：LaunchAgent（~/Library/LaunchAgents）在**用户登录时**拉起，
不是开机即起——机器重启后停在登录界面则采集器不跑。要无人值守：系统设置里
开自动登录，或把 plist 改装到 /Library/LaunchDaemons（须去掉对用户级代理的
依赖，出网代理不可用时 WS 会走直连策略档）。VPS + systemd（`enable` 即开机
自启）没有这一层麻烦，仍是治本项。

**币安 IP 封禁期行为（内建，无需干预）**：采集器读 `jarvis_net` 封禁登记
（`~/.vibe-trading/net_ban.json`），封禁期内种子/缺口回补 REST **显式静默**
（只走 WS 采集 + tape 分钟 bar 照常落库；WS 不受 REST 封禁影响），失败冷却
自动对齐封禁到期时刻，解禁后 ≤60s 自动重播种/回补，全程无人工介入。

## 3. Linux systemd 常驻（VPS 迁移后的目标形态）

`/etc/systemd/system/jarvis-collector.service`（把 `/opt/vibe-trading` 换成实际路径，
`User` 换成运行账号）：

```ini
[Unit]
Description=Jarvis T10 signal collector (WS closed-bar resident capture)
After=network-online.target
Wants=network-online.target

[Service]
Type=simple
User=jarvis
WorkingDirectory=/opt/vibe-trading
ExecStart=/opt/vibe-trading/.venv/bin/python /opt/vibe-trading/jarvis_signal_collector.py run
Restart=always
RestartSec=10
# 业务日志进程自写 ~/.vibe-trading/*.log；journal 只兜底裸错误
StandardOutput=journal
StandardError=journal
# 数据库若同机跑 pg，可加 After=postgresql.service

[Install]
WantedBy=multi-user.target
```

```bash
sudo systemctl daemon-reload
sudo systemctl enable --now jarvis-collector    # 开机自启 + 立即启动
systemctl status jarvis-collector               # 验活
journalctl -u jarvis-collector -f               # 兜底日志
tail -f ~/.vibe-trading/jarvis_signal_collector.log   # 业务日志
```

VPS 迁移要点（决策与采购归用户，这里只列技术前置）：
- 数据库：VPS 上的采集器要与本机看同一个库才能合并覆盖——推荐 VPS 跑 pg，
  本机 `~/.vibe-trading/db.json` 指向它（迁移步骤见 `docs/pg-ops.md`、
  `jarvis_migrate_to_pg.py`）；否则两边各写各库，报表口径分裂。
- 出网：VPS 自带独立出口 IP，顺带解决共享代理 IP 被币安封禁的问题
  （`jarvis_net` 的封禁登记/权重刹车照常生效）。
- 不需要 GUI/dashboard：采集器是独立进程，不依赖面板。

## 4. 运行语义速查（排障用）

- **只认已收盘 bar**：kline 流 `k.x=true` 才入队，进行中 bar 一律丢弃；
  `record_batch(now=bar 收盘时刻, price=bar 收盘价)`。
- **REST 纪律**：常态零轮询。仅三处按需出网（种子 300 根/对一次性、断线缺口
  回补单次一拉、可选基差 300s TTL），全部经共享分钟预算/封禁登记/权重刹车。
- **双写入者**：dashboard 面板开着时也在写。采集器写库前查 `twelve_signal_state`
  最大 `updated_ts`，bar 收盘时刻不比它新就只记台账（`note=state_fresher`），
  不回退状态、不制造 flip-flop 假变更。
- **套利腿**：`sigcol_basis=true`（默认）拉期现基差；取数失败/关闭时直接剔除
  arbitrage 信号（不硬造「数据不足」中性去污染变更流水）。
- **队列**：收盘事件先入队（容量 `sigcol_queue_max`），工作线程慢慢算；
  4h 边界所有 TF 同时收盘的突发由队列吸收，WS 线程永不被计算阻塞。
- **流水上限**：`twelve_signal_changes` 保留 `signal_history_max_rows`
  （默认 50 万，旧值 5 万已被打满在吃 T9 历史尾部）。
- **验收口径**：`coverage --days 7` 的 acceptance 段；台账有效窗口不足 7 天时
  如实标注「未达」，不虚报。

## 5. 配置键一览（jarvis_config，三处已登记）

| 键 | 默认 | 说明 |
| --- | --- | --- |
| `sigcol_enabled` | true | 总开关（关=进程启动即退出） |
| `sigcol_symbols` | ""（空=跟随 watchlist） | 采集标的集，与 watchlist 解耦扩容（恢复历史 9 标的用它） |
| `sigcol_tfs` | "5m,15m,30m,1h,4h" | 采集 TF 集 |
| `sigcol_backfill_bars` | 288（0~480） | 断线缺口单 (币,TF) 最大回补根数；0=关回补 |
| `sigcol_queue_max` | 4096 | 事件队列容量 |
| `sigcol_ledger_retention_days` | 45 | 台账保留天数 |
| `sigcol_tape` | true | 同进程挂 aggTrade→tape 分钟 bar 链 |
| `sigcol_basis` | true | 期现基差（套利腿）；关=剔除 arbitrage |
| `signal_history_max_rows` | 500000 | 变更流水保留上限（约 1KB/条，留意磁盘） |
