# 12 信号模拟盘「亏损止血」— 优化开发计划

> 版本：v1.0 | 日期：2026-08-06 | 起草：全栈工程师会话（需求分析 + R10 取证归因）
>
> **目标**：基于 406 笔已平仓交易的亏损归因取证（净亏 -176.22U / 胜率 26.6%），
> 按置信度从高到低逐一堵住结构性出血点，把 12 信号模拟盘从「负期望自动亏钱机」
> 改造为「费用感知、风险受控、劣质信号自动熔断」的正期望验证场。
>
> **面向执行工程师**：本计划假设你对仓库零上下文。每个任务给出精确文件路径、
> 契约、验收命令。实现顺序 = 任务编号顺序；每任务独立 commit，不 push。

---

## 〇、当前进度（断电后从这里接着做）

> 规则：每完成一项，把对应任务的 ⬜ 改成 ✅，并在「最近一次改动」补一行摘要。

- **当前阶段**：S1-S7 全部完成（门禁链：S4 周期→S5 逆势→S3 熔断→S1/S2 参数；口径：手续费+资金费）
- **下一步要做的任务**：无——观察 7 天后用 S6 报表复盘效果，数据说话再定第二轮
- **已完成任务数**：7 / 7
- **最近一次改动**：2026-08-06 S7 完成——`twelve_funding_rate`(0.0001/8h) 配置化；`_do_close` 非爆仓平仓按持仓每满 8h × entry 名义计提资金费（rate>0 多头付/空头收），折进净 pnl 并落 trade 新列 `funding_fee`（ALTER 幂等升级）；smoke 新增 S7a-c（多单 3 期支出/空单 1 期收入/不足 8h 不计提）全绿
- **更早改动**：2026-08-06 S5 完成——`twelve_trend_filter_enabled`(默认开) 配置化；`_pre_gate` 接入 `_trend_context`（复用 jarvis_wyckoff.analyze 1h 进程内指纹缓存，不新增出网）：dist-C/D/E 拒多、acc-C/D/E 拒空，仅 5m/15m/30m 生效，拒单 counter_trend；威科夫异常/ok:false/stale 一律放行不阻塞；smoke 新增 S5a-f（逆势拒/顺势放/1h 不拦/acc 拒空/异常与 stale 放行/开关关闭放行）全绿
- **更早改动**：2026-08-06 S4 完成——`twelve_tf_enabled`(全开)/`twelve_tf_min_confidence`(5m=0.75 其余 0) 配置化；`_pre_gate` 链首加周期门禁（TF 停用或信号强度低于该 TF 置信档 → 拒单 tf_gate）；smoke 新增 S4a-d（5m 低置信拒/15m 同置信放行/5m 高置信放行/关 TF 全拒）全绿
- **更早改动**：2026-08-06 S3 完成——`twelve_cb_window`(30)/`twelve_cb_min_trades`(10)/`twelve_cb_min_winrate`(15)/`twelve_cb_max_loss`(10U)/`twelve_cb_cooldown_hours`(24) 五键配置化；新表 `twelve_sim_breaker` 持久化熔断态；`_pre_gate` 信号级前置门禁接入 开仓/挂计划/成交 三路径，`_do_close` 尾接 `_breaker_on_close` 簿记（滚动窗口评估触发 → tripped 拒单 circuit_breaker → 冷却期满半开 1 笔试探 → 赢恢复 reset_ts 重起算 / 输续熔断 trip_count+1）；事件落 signal_log(change_kinds=breaker)；smoke 新增 S3a-f（触发/拒单防重/半开/试探在途仍拒/输续熔/赢恢复+恢复后放行）全绿
- **上上次改动**：2026-08-06 S2 完成——`twelve_min_rr`(1.5) / `twelve_fee_burden_mult`(3) 配置化；`_risk_gate` 追加 rr_too_low / fee_negative_ev 两道拒单（费率复用 twelve_sim_fee_pct，双边×杠杆占保证金口径）；smoke 新增 S2a-d（RR 1.2 拒、费用负担拒、正常放行、min_rr 热加载）全绿
- **更早改动**：2026-08-06 S1 完成——`twelve_min_sl_pct` / `twelve_auto_lev_loss_frac`(0.25) / `twelve_max_leverage`(按 TF 分层) 配置化热加载；`_risk_gate` 拒单落 `status='rejected'`+`reject_reason` 行 + signal_log 留痕（防重 + 7 天保留）；显式杠杆夹 TF 上限；smoke 新增 S1a-e（含 YAML 热加载用例）全绿
- **更早改动**：2026-08-06 S6 完成——`jarvis_dashboard.py` 新增 `GET /api/twelve/attribution`（1h 缓存，`?days=7`）：信号系统×TF×平仓原因 聚合 净利/胜率/费用/笔数 + 门禁 rejected 统计（S1-S5 未落库时计数 0 带 note）；curl 实测 ok:true，聚合与手工 SQL 对账一致（406 笔 / -176.22U / 费 77.64U）

---

## 一、取证结论摘要（R10 · 2026-08-06 · 只读取证）

数据源：PostgreSQL `jarvis` 库 `twelve_sim_trade`，406 笔已平仓（8/03-8/06），全部
ETHUSDT。净亏 -176.22U，日均 -44U（用户体感「日亏 200+」实为 4 天累计的体感）。
**注意：数据几乎全部是 R3 改造前旧口径**（market 立即开仓 + SL/TP 跟随信号改写）。

| # | 亏损主因假设 | 证据 | 置信度 |
| --- | --- | --- | --- |
| 1 | 止损距离过窄 × 自动杠杆顶格 20× | sl 平仓 228 笔（56%）胜率仅 4.4%、合计 -209.15U（超总净亏）；5m 中位 SL 距离 0.172% 在噪声带内；`_auto_leverage=floor(0.5/dist)` 导致 402/406 笔全 20×，一打 SL 亏保证金≈50% | 0.90 |
| 2 | 手续费侵蚀 | 双边 0.05%×20× 杠杆放大名义至笔均 191U，总费 77.64U = 净亏 44%；5m 轴费用占该轴亏损 66% | 0.85 |
| 3 | 5m 短周期权重过重 + 信号高频翻动 | 5m 占 202/406 笔亏 -57.7U；oscillator 4 天信号变更 3383 次 | 0.70 |
| 4 | 弱系统无战绩熔断 | elliott 胜率 7%、最大连亏 38 笔不停；triple_rsi 一家亏 -57.6U（笔数是别人 2-6 倍） | 0.65 |
| 5 | 空头逆势 + 旧口径 SL/TP 漂移 | short -123.2U vs long -53.1U，取证窗口 ETH 1830→1919 上行；持仓期 SL/TP 被改写 483 次（R3 已改仓位独立，待新数据验证） | 0.50 |

已排除：入场价 vs 推荐点位偏差中位 0.000%（追价成本可忽略）——**亏钱不在开仓价差，
在 SL/TP 结构与费用**。

---

## 二、任务总表

| 编号 | 状态 | 任务 | 对应主因 | 优先级 | 预估 |
| --- | --- | --- | --- | --- | --- |
| S1 | ✅ | 止损最小距离门禁 + 杠杆与止损解耦 | #1 | P0 | 0.5d |
| S2 | ✅ | 费用感知期望值门禁（最小盈亏比） | #2 | P0 | 0.5d |
| S3 | ✅ | 信号×周期战绩熔断器 | #4 | P0 | 1d |
| S4 | ✅ | 周期再平衡：5m 严门禁 | #3 | P1 | 0.5d |
| S5 | ✅ | 高周期趋势逆势过滤（复用威科夫 Phase） | #5 | P1 | 0.5d |
| S6 | ✅ | 归因报表固化 + R3 新口径复盘 | 全部 | P1 | 1d |
| S7 | ✅ | 资金费率模拟（可选） | 补口径 | P2 | 0.5d |

> 公共纪律：所有开关默认值必须能在 `jarvis_config.py` 配置（DEFAULTS + GROUPS 双写，
> YAML 热加载生效）；被门禁拦下的信号**不静默丢弃**，落 `status='rejected'` +
> `reject_reason`，可复盘；每任务扩 `_twelve_sim_smoketest.py` 用例；不新增出网请求；
> 只 commit 本次相关文件，不 push。

---

## 三、逐任务契约

### S1 · 止损最小距离门禁 + 杠杆与止损解耦（P0）

**文件**：`Vibe-Trading/jarvis_twelve_trader.py`（`_auto_leverage`、`_resolve_entry_params`）、
`jarvis_config.py`

**问题**：`_auto_leverage = floor(AUTO_LEV_SL_LOSS_FRAC=0.5 / 止损距离)` 夹到
`MAX_AUTO_LEVERAGE=20`——止损越窄杠杆越顶格，等于「专挑噪声带下最大注」。

**改法**：
1. 新配置 `twelve_min_sl_pct`（按 TF 分层，默认 `{"5m":0.5,"15m":0.7,"30m":1.0,"1h":1.2,"4h":2.0,"1d":3.0}`，单位 %）。
   开仓前校验 SL 距离 < 下限 → 拒单，`reject_reason='sl_too_tight'`。
2. `AUTO_LEV_SL_LOSS_FRAC` 0.5 → 配置化 `twelve_auto_lev_loss_frac` 默认 **0.25**
   （打到 SL 亏保证金 25%）；`MAX_AUTO_LEVERAGE` → 配置化 `twelve_max_leverage`
   按 TF 分层默认 `{"5m":5,"15m":8,"30m":10,"1h":12,"4h":15,"1d":20}`。
3. plan/配置显式给了杠杆仍尊重显式值（只夹 TF 上限）。

**验收**：smoke 用例——5m SL 距离 0.3% 拒单；5m SL 0.8% 放行且杠杆 ≤5；显式杠杆
夹到 TF 上限；配置改动热加载生效。

### S2 · 费用感知期望值门禁（P0）

**文件**：同 S1。

**改法**：开仓前算 `rr = (TP距离) / (SL距离)`、
`fee_pct = 双边费率 × 2 × 杠杆`（占保证金），要求：
1. `rr ≥ twelve_min_rr`（默认 1.5）；
2. `TP距离 × 杠杆 ≥ fee_burden_mult × fee_pct`（默认 3——单笔止盈至少覆盖 3 倍费用）。
   不满足 → `reject_reason='fee_negative_ev'` / `'rr_too_low'`。
费率常量若引擎已有则复用，没有则 `twelve_fee_rate` 默认 0.0005 配置化。

**验收**：smoke——TP/SL 比 1.2 拒单；构造费用占比超阈值用例拒单；正常单放行。

### S3 · 信号×周期战绩熔断器（P0）

**文件**：`jarvis_twelve_trader.py`、`jarvis_config.py`

**改法**：按 `(signal_system, timeframe)` 维度维护滚动战绩（最近
`twelve_cb_window` 默认 30 笔已平仓）：
1. 胜率 < `twelve_cb_min_winrate`（默认 15%）**且** 净亏 < `-twelve_cb_max_loss`
   （默认 10U）→ 熔断：该组合只推送信号不开仓，`reject_reason='circuit_breaker'`；
2. 冷却 `twelve_cb_cooldown_hours`（默认 24h）后自动半开：放行 1 笔试探，赢了恢复，
   输了继续熔断；
3. 熔断状态落库（新表或复用 kv），进程重启不丢；熔断/恢复事件写 signal_log 可审计。

**验收**：smoke——注入 30 笔连亏后第 31 笔拒单；冷却后半开放行 1 笔；赢单恢复。
elliott（胜率 7% 连亏 38 笔）类场景在回放中会被拦。

### S4 · 周期再平衡：5m 严门禁（P1）

**文件**：同 S3。

**改法**：新配置 `twelve_tf_enabled`（默认全开）与 `twelve_tf_min_confidence`
（5m 默认要求信号置信/强度达到更高档，具体字段以引擎现有信号结构为准）。
5m 不直接禁用（保留验证价值），但需同时满足 S1/S2/S3 门禁 + 更高置信档。

**验收**：smoke——5m 低置信拒单 `reject_reason='tf_gate'`；15m 同置信放行；
配置关掉 5m 后该 TF 全拒。

### S5 · 高周期趋势逆势过滤（P1）

**文件**：`jarvis_twelve_trader.py`（复用 `jarvis_wyckoff.analyze` 的进程内缓存，
禁止新增出网）。

**改法**：开仓前取 1h 威科夫 state：`markdown/dist-C/D` 阶段拒多头短周期单、
`markup/acc-C/D` 拒空头短周期单（仅 5m/15m/30m 生效，1h 及以上不拦），
`reject_reason='counter_trend'`。开关 `twelve_trend_filter_enabled` 默认开；
威科夫数据 stale/不可用时**放行不阻塞**（可用性优先，与 seatbelt 同哲学）。

**验收**：smoke——mock 1h dist-C 时 5m 多单拒、5m 空单放行；wyckoff 异常时放行。

### S6 · 归因报表固化 + R3 新口径复盘（P1）

**文件**：`jarvis_dashboard.py`（新 `GET /api/twelve/attribution`，1h 缓存）、
RuoYi 侧看板可后续接。

**改法**：把 R10 的取证 SQL 固化成接口：按 信号×TF×平仓原因 聚合净利/胜率/费用/
笔数 + rejected 统计（S1-S5 各 reject_reason 的拦截量），支持 `?days=7`。
上线 7 天后用该接口复盘 R3 新口径 + S1-S5 效果，数据说话再定第二轮。

**验收**：curl 实测 ok:true；聚合数与手工 SQL 对账一致；无新增出网。

### S7 · 资金费率模拟（P2 · 可选）

持仓每 8h 按 `twelve_funding_rate`（默认 0.0001）计提，落 trade 表新列
`funding_fee`。补齐口径用，收益低，最后做。

---

## 四、验收与回归

- `python3 _twelve_sim_smoketest.py` 全绿（每任务新增用例累积）
- `python3 -m py_compile jarvis_twelve_trader.py jarvis_dashboard.py` 通过
- `_wyckoff_smoketest.py` / `_supply_demand_smoketest.py` 零回归
- 观察指标（S6 报表）：sl 平仓占比 56%→<35%、笔均费 0.19U→<0.08U、
  周净利转正为第一目标；样本积累 ≥7 天再评判
