# 贾维斯出网预算审计 + 新标的接入评估（SNDK/XAU/SPCX）

> 日期：2026-08-13 | 取证：BajieAsk-agent-7（全栈工程师）| 性质：静态审计 + 一次性 exchangeInfo 验证（封禁解除后）
> 红线遵守：14:10 封禁解除前零 REST 出网；只改 `jarvis_net.py` / watchlist 配置 / 本任务新文件
> 官方依据：USDⓈ-M 合约 `REQUEST_WEIGHT` 限额 **2400 权重/分/IP**（developers.binance.com · Common Definition，已核实）；429 后不退避/反复violate → 418 IP 封禁且**对惯犯阶梯加时**

---

## 一、出网调用点全景（调用方 × 端点 × 权重 × 频率）

### 1.1 Python 侧（统一入口，已全员进闸）

所有 Python 出网汇聚到 `jarvis_crypto_data._get()`（jcd:273-375）三道闸：**端点 TTL 缓存 → 封禁/冷却短路（jarvis_net.banned_until）→ 权重水位刹车（X-MBX-USED-WEIGHT-1M ≥1200 停手，jcd:311-316）→ 跨进程共享分钟预算（`budget_take`，权重计费，jcd:317）**。429 尊重 Retry-After（≥90s 全进程冷却，jcd:340-350）；418/-1003 解析 "banned until" 登记共享封禁（jcd:255-270）。

| 调用方 | 端点（权重） | 驱动/频率 | TTL 摊薄后典型消耗 |
| --- | --- | --- | --- |
| dashboard `/api/kline`（面板 K 线） | fapi/v1/klines（limit≤499→2） | 桌面面板轮询 | ~6 TF/30s TTL ≈ 12-24 权重/分（峰值） |
| dashboard twelve 重算/consensus | klines ×6TF（2/次） | 面板信号页，180s 源侧缓存 | ≈ 1-4 权重/分 |
| jarvis_market_intel | premiumIndex 全量(10)/OI hist(1)/多空比(1×2) | 300s TTL（sync market_snapshot 组驱动） | ≈ 2.8 权重/分 |
| jarvis_daemon（日线+4h intraday） | klines/price 若干 | 日线 1 次/日 + 4h 对齐 | ≈ 0.1 权重/分 |
| jarvis_price_alert / sentiment 等 | ticker/price(1) | 零星 | ≤ 2 权重/分 |
| jarvis_orderbook / depth_view | fapi/v1/depth limit=1000（20） | 仅 WS 断档 resync | 突发 20/次，稳态 0 |
| jarvis_factor_backtest | klines 分页 | 手动回测才跑 | 按需 |
| **Python 侧总闸** | — | `rest_max_per_min = 40`（运行时实配） | **硬顶 40 权重/分 = 官方配额 1.7%** |

> 结论 1：Python 侧**不可能**自己撞 2400——需求侧合计 ≈30 权重/分，且被 40/min 共享文件闸（`~/.vibe-trading/net_budget.json`，fcntl 互斥）硬性封顶，超出即降级走缓存。
> 结论 2：历史上的「未记账大户」（market_intel 裸 requests、按次数不按权重计账）已在 2026-08-09 加固中收编（market_intel:45-59 注释自述）。

### 1.2 桌面端（第二本账，未与 Python 共享预算）

`desktop/src/lib/footprint/binanceFeed.ts` renderer 直连 fapi：**常规链路走后端 seed + WS 流（aggTrade/kline，0 REST 权重）**；仅后端不可用时兜底直连回填——klines limit=1500（权重 10/页）+ aggTrades limit=1000（权重 20/页）×上限 10 页（`AGG_TRADES_MAX_PAGES=10`，realDataService.ts:64）＝**单次图表兜底突发 ≈200-260 权重**。已有自律：429/418 模块级短路 + 尊重 Retry-After + 解封敏感期随机错峰。
**缺口：与 Python 侧是两本独立的账**——同一出口 IP 两套预算互不知情，理论并发峰值 = 40 + 260 突发。

### 1.3 第三方流量（共享代理出口 IP，本次封禁主因）

出口 IP `103.62.49.178` 为共享代理——**别家租户的币安流量与我们计同一个 2400/min**。证据见 §二。

---

## 二、本次封禁（至今天 14:10）直接原因定案

时间线（`~/.vibe-trading/jarvis_data_degrade.log` 逐条 + `net_ban.json`）：

| 时刻 | 事件 | 证据含义 |
| --- | --- | --- |
| 8/12 18:20-8/13 09:14 | 本机休眠，零出网（引擎日志断档 15h） | 本机不可能贡献流量 |
| 09:17:03 | 醒机即见「IP(103.62.49.178) banned until 09:39:52」 | **封禁发生在本机睡眠期间——第三方流量所致，铁证** |
| 09:35-10:01 | 自家水位刹车反复拦截（响应头回报 IP 已用权重 ≥1200，而自家闸仅 40/min） | 共享 IP 持续被别家打到高位 |
| 09:39:52→09:39:57 | 封禁一到期，5 秒内再 418（延至 09:53:59） | **到期齐发+IP 仍高热 → 秒回封** |
| 09:54:21 | 一秒内三条封禁登记（09:55:59 / 09:59:59） | 多进程并发撞墙被分别回执 |
| 10:05:29-30 | 429 ×2（Retry-After 冷却 90s 已生效） | 升级前最后警告 |
| 10:07:31-32 | 三连 418：banned until 14:01 / 14:03 / **14:09**（≈4 小时） | 币安惯犯阶梯加罚落锤 |

**定案**：
1. **主因＝共享代理出口 IP 的第三方流量**把 fapi 2400/min 打满（我们睡着时就被封了；醒后自家 40/min 闸+1200 水位刹车全程在拦自家请求）。
2. **放大器＝封禁到期瞬间多进程齐发**：`banned_until()` 到期即全量放行，daemon/dashboard/sync/twelvesim 同时恢复 → IP 仍高热 → 立刻再 418 → 触发官方「repeat offenders 阶梯加时」直至 4h。
3. Retry-After 尊重、429 全局熔断、权重计费、跨进程预算**都没被绕过**（本次不是这些环节的锅）；桌面端兜底回填今晨未活跃（无对应流量证据），列为结构性风险而非本次成因。

**已落地修复（本任务 commit `1ee3fc5`）**：`jarvis_net.py` 新增**封禁到期软着陆层**——到期后只放一支「探针」试路（其余进程拿合成截止继续走缓存），探针存活满 90s 确认窗才全面恢复；再次撞禁按 2^n 阶梯推迟下次试路（60s 起、15min 封顶，配置键 `net_probe_confirm_s`/`net_reban_window_s`/`net_reban_pad_base_s`/`net_reban_pad_cap_s`，暂读运行时配置、三处登记待 T2 WIP 收敛后补）。对调用方零改动透明生效；`_net_postban_smoketest.py` 14/14、`_net_budget_smoketest.py` 回归 9/9 全过。**运行中进程需下次重启后生效（不在本任务内重启）。**

**遗留跟进（超出本任务文件权限，建议派单）**：
- 桌面端与 Python 共享预算（回填改走 dashboard 代理，或 Electron 主进程读写 `net_budget.json` 同一本账）；
- 根治＝专用出口 IP/VPS（与正期望重建计划 T4「封禁排查结案」、T10「常开采集机」同一笔投入）。

---

## 三、新标的 SNDK / XAU / SPCX 接入

### 3.1 机制盘点（静态已完成）

- **watchlist 配置**：运行时 `~/.vibe-trading/jarvis_config.json` 的 `watchlist` 键覆盖 `jarvis_config.py DEFAULTS`（:64-65——**默认表本就含 8 标的**：BTC/ETH/SNDK/SKHY/SPCX/XAU/CL/BZ，2026-08-05「用户关注 8 品种」收敛时加入；当前运行时被覆盖为 `[BTCUSDT, ETHUSDT]`）。`load()` 按 mtime 热加载。
- **符号归一化**：`fetch_klines_df`/`mark_price`/`check_symbol` 统一 `.upper()` 去 `-`/`/`、无后缀自动补 `USDT`（jcd:538-540/614-616/676-678）→ `SNDK→SNDKUSDT`、`XAU→XAUUSDT`、`SPCX→SPCXUSDT` 均正确。⚠ 传 `XAUUSD` 不会补成 USDT（endswith("USD") 直接放行）——统一用完整符号。
- **合约独有符号**：项目已注明（jcd:634 注释）SNDK/SKHY/SPCX/XAU/CL/BZ **现货域无此对、仅 USDⓈ-M 合约有**——fapi 域封禁/WS 回退现货期间这些符号拉不到数据（历史证据：XAUUSDT 94 条信号至 8/07、SNDKUSDT 27 条至 8/05、SPCXUSDT 在 twelve_state.json 有 8/05 状态——都曾接入过）。
- **接入预检设施**：dashboard `/api/symbol/check`（jcd.check_symbol，2026-08-05 新功能）就是为这类符号建的两域可用性预检。

### 3.2 exchangeInfo 验证（14:10 封禁解除后执行）

fapi `exchangeInfo` 不支持 symbol 过滤（jcd:673 注释）→ **一次全量调用（权重 1）验证全部三个符号**，经 `jcd._get` 三道闸出网。

> 【待执行】封禁 14:10 解除后执行，结果回填 §3.4；执行前本节无结论。

### 3.3 下游覆盖与需手动配置的点

| 环节 | 跟随方式 | 需手动？ |
| --- | --- | --- |
| 数据层/面板（klines、symbol/check、市场页） | 热跟随 watchlist（每次 get 重读） | 否 |
| market_intel 资金费展示 | `_display_symbols()` 每次调用读 watchlist | 否 |
| sync 同步器 `resolve_symbols` | 启动时读 watchlist | **重启 sync 生效** |
| daemon 简报/intraday | 启动时读 watchlist（:521-526） | **重启 daemon 生效** |
| WS 常驻采集器（agent-6 在建） | 按任务背景：跟随 watchlist | 待 agent-6 交付确认 |
| twelve 模拟盘引擎 | 独立 per-symbol 开关：`twelve_sim_config` 表（当前仅 ETHUSDT enabled=1） | **手动 `python jarvis_twelve_trader.py config-set SNDKUSDT` 等逐个启用**（涉 PG 写+开仓决策，留给用户/运维确认） |
| 若依侧展示 | 纯消费镜像数据，跟数据走 | 否 |

### 3.4 验证结果与接入动作（14:10 后回填）

> 【待回填】此节在 exchangeInfo 实测完成前**不含任何结论**。历史信号（XAU 94 条/SNDK 27 条/SPCX 有状态）只证明「曾经拉到过数据」，不能替代交易所当前上架状态的权威验证——若某符号已下架/停牌（status≠TRADING），如实报告不硬接。

---

## 四、+3 标的出网预算推演

### 4.1 现状轮询模式（面板/同步驱动）

新增需求（全部经 40/min 闸）：
- 面板同时开 3 个新标的 K 线：+36-72 权重/分（30s TTL 摊薄前）
- market_intel：premiumIndex 本就全量（10 权重含所有符号，**零增量**）；OI/多空比 +1×3/300s ≈ +0.6/分
- twelve 重算（若面板轮询新标的）：+4/分·标的

**合计需求 ≈ +45-85 权重/分，但被 `rest_max_per_min=40` 硬顶封死**——表现为更频繁降级走缓存/数据变钝，**不会撞 2400、不会封禁**。若想吃饱，可将闸上调至 1200（=官方 50%，与水位刹车 `rest_weight_brake=1200` 对齐，也与正期望重建 T4「出网权重 < 50% 配额」验收口径一致）；即便按满 1200 算，8 标的轮询需求 ≈150-250 权重/分，仍只占官方 10%。

### 4.2 WS 采集器模式（agent-6 在建，官方推荐方向）

- 稳态：aggTrade/kline 流全走 WS，**REST 权重 ≈ 0**（币安封禁文案原话「Please use the websocket for live updates to avoid bans」）
- 一次性：每标的暖启动 klines 6 TF ×2 权重 ≈ 12 权重/标的（仅重连时）
- 流数：≈7 流/标的（aggTrade + 6 TF kline）；fstream 单连接上限 200 流

### 4.3 安全边界结论

| 模式 | 安全标的数上限 | 约束来源 |
| --- | --- | --- |
| 轮询（闸 40/min） | ~5 个（再多面板数据显著变钝） | 自家闸，非官方配额 |
| 轮询（闸放宽 1200） | ~25 个 | 官方 50% 配额线 |
| WS 采集器 | ~20-28 个/连接（警戒 <140 流） | fstream 流数上限，非权重 |

**「加了会不会再封」**：不会由我们自己触发——三闸+软着陆层护着，自家最坏 40 权重/分 + 桌面兜底突发 260，距 2400 一个数量级。**真正的封禁变量是共享出口 IP 的第三方流量**（本次封禁在我们睡觉时发生），与标的数无关；根治只有专用 IP。

---

## 五、证据缺口（诚实声明）

- 桌面端今晨是否活跃无日志可考（renderer console 不落盘）——兜底回填的实际触发频率未量化；
- 第三方流量规模只能从「醒机即封 + 水位 ≥1200 而自家闸 40」反推，无法直接观测共享 IP 上别家请求；
- `agent/` 目录的 binance SDK connector 未审计运行态（无进程在跑，静态判定不活跃）；
- klines/depth 权重分档取自项目 `_req_weight` 表（jcd:173-200，自述「按官方文档估算宁高勿低」）与官方 legacy 文档交叉核对一致；developers.binance.com 新版页面有反爬未能逐字引用，2400/min 与 exchangeInfo=1、aggTrades=20 已经检索核实。
