# 贾维斯「正期望重建」— 终极开发计划

> 版本：v1.0 | 日期：2026-08-11 | 起草：主控 Opus 5-3（PM / 执行微结构派）× Opus 5-8（系统概率派）
>
> **来源**：两位资深交易员就同一份真实台账（433 笔已平仓 / 净 -183.16U）展开**三轮**对抗式辩论，
> 各自独立跑 SQL 取证、互相反驳并公开认输——**双方开局的核心论据最终都被自己推翻**。
> 本文是辩论的裁决书与施工图，不是折中方案。所有关键数字均由两人**独立复算**过。
>
> **面向执行工程师**：本计划假设你对仓库零上下文。每个任务给出精确文件行号、验收阈值、
> 回滚点。每任务独立 commit，不 push。
>
> **前置契约**：《贾维斯-13诊断驱动-策略进化开发计划.md》《贾维斯-12信号亏损优化-开发计划.md》

---

## 〇、当前进度（断电后从这里接着做）

- **当前阶段**：T9 评估台已落地并跑出首轮结果；T10 因 T9 的实测发现而新增；
  **T8 振幅门槛已落地并出首轮达标表；T7 零成交五套根因已定案**
- **下一步**：**T10（采集覆盖率补齐）**——T9 已证明覆盖偏斜与被测量同一数量级，
  不补齐则即使样本攒够、排名也不可信。与「专用出口 IP」是同一笔投入
- **已完成任务数**：3 / 10（T9 工具已交付结论待样本；T8 已交付含首轮结果；T7 诊断已定案）
- **最近一次改动**：2026-08-13 T8 落地 `jarvis_twelve_amplitude.py`（纯函数模块，
  口径预登记 A1-A7 锁在模块头）+ `GET /api/twelve/attribution` 恒增 `amplitude` 键；
  T7 五套零成交根因定案（3 套设计恒 neutral / gann 无点位被静默跳过、且信号篇 P2-5
  已将其方向降级恒 neutral / gap 在成交标的上无信号），配置显式标记
  `twelve_system_enabled`，attribution 增 `systems_note` 真实口径。
  首轮 T8 达标表（436 笔 / 77 独立赌注 / 32 参检格）：**0 达标 / 3 不达标 / 32 不可判定**，
  不达标 = chanlun|5m、triple_rsi|5m、triple_rsi|15m（进观察名单，未自动停用）
- **上一次改动**：2026-08-11 T9 落地 `jarvis_signal_floor.py`（491 行，只读，
  五项预登记锁在模块头 + P6 时段分层 + 按日聚类主口径 + 朴素口径对照）。
  首轮 34 个单元格全部 INSUFFICIENT；最接近地板的是 **oscillator·欧洲盘 +0.0926%**（地板 0.1000%）。
  副产品：朴素逐笔口径把显著性**中位虚高 2.4×**（oscillator 最高 4.1×），坐实 P5 预登记的必要性

---

## 一、取证基线（2026-08-11 · 双人独立复算 · 只读）

数据源：本地 PostgreSQL `jarvis` 库 `twelve_sim_trade`，433 笔已平仓（8/03–8/10），全部 ETHUSDT。
`twelve_sim_position` 823 笔拒单。两人分别独立跑 SQL，四项支点数字完全一致。

| 指标 | 数值 |
| --- | --- |
| 已平仓 / 净盈亏 | 433 笔 / **-183.16 U** |
| 胜率（pnl>0 口径） | 27.7%（120 笔） |
| 平仓构成 | sl 240 笔 -225.98U / flip 108 笔 +1.19U / tp 79 笔 +40.97U / timeout 6 笔 +0.66U |
| 双边手续费合计 | **81.31 U**（占净亏 44.4%，已扣进 pnl，见 `jarvis_twelve_trader.py:1364`） |
| 止损结算劣于计划位合计 | **51.35 U**（240 笔止损中 134 笔，均劣 0.1998%） |
| 摩擦合计占净亏 | **72.4%** |
| 风险归一净期望（总额比） | **-0.518 R** |
| 风险归一剔摩擦期望 | **-0.143 R** |
| 累计拒单 | 823 笔（sl_too_tight 532 / tf_gate 130 / rr_too_low 119 / counter_trend 32 / circuit_breaker 9 / sl_below_atr 1） |
| 当前成交速率 | **0 笔/天**（8/05 尚有 146 笔/天） |
| 毛价格位移（台账原值） | -0.1096%（t=-3.77） |
| 毛价格位移（**T1 纠偏后**） | **-0.0478%（t=-1.97）**；独立赌注口径 **-0.0229%（t=-0.40）** |
| 往返手续费（按名义，与杠杆无关） | **0.1000%** |

> 口径说明：R = 该笔计划止损亏损额（`|entry−stop_loss|×qty`）。跨周期比较必须用 R 归一，
> 因为 4h 单笔风险是 5m 的 6 倍，直接比 U 会系统性冤枉高周期。
> 均值口径受 47 笔「止损距离 < 0.05%」的极端单严重带偏，全文一律用**总额比**（Σ 分子 / Σ 分母）。

---

## 二、七条裁决（前五条为分歧裁决，后两条为辩论中新生成的结论）

### 裁决 1 · 「已实现结果是不是显著为负」→ **是，已定案**

按「同一小时 × 同方向」归并，433 笔压成 **76 个独立赌注**（平均并发持仓 17.3、峰值 24，全压在单一 ETH 上）。
按此口径检验已实现盈亏：**均 -2.410 U/赌注，t=-4.78，p<1e-5**。

即便剔除全部已识别摩擦，剩余 **-0.143 R** 仍为负。

> **注意措辞**：本条成立的是「**已实现结果**显著为负」，**不等于**「信号方向能力为负」。
> 两者的区别由裁决 6 给出，且那条推翻了双方的开局立场。

### 裁决 2 · 「摩擦是不是主因」→ **是主因但不是全部，双方各对一半**

摩擦占已实现亏损 **72.4%**，修完期望值从 -0.518R 改善到 -0.143R（改善 72%），
但**不足以转正**。乙方主张的「信号未死」成立于「亏损幅度」，不成立于「期望符号」。

### 裁决 3 · 「止损距离是不是有效旋钮」→ **不是，乙方认输**

按现行 S1 阈值把 433 笔切成 A（宽止损 69 笔）/ B（窄止损 364 笔）：

```
A: 胜率 0.507 × 均盈 +0.887 + 败率 0.493 × 均亏 -1.784 = -0.4292
B: 胜率 0.234 × 均盈 +0.541 + 败率 0.766 × 均亏 -0.715 = -0.4218
```

胜率差 27.4 个百分点、盈亏幅比差 0.497 vs 0.756，**乘积回到同一个数**（差 -0.0073U，t=-0.034，p=0.973）。
止损距离只是在**同一条等期望线上滑动**：放宽则胜率升、赔率降，反之亦然。

> 自我约束：不声称「已证明两组等价」。TOST 等价检验 ±0.1U 下 p=0.335、±0.2U 下 p=0.188 均不达标，
> 95%CI 为 [-0.434, +0.419]。能站住的结论是：**胜率与赔率的差异真实且已被充分检出，而其乘积没有跟着动。**

**推论**：S1 用止损距离当判据拒掉了 65% 的拒单量（532/823），而该判据在自己的历史样本上
区分盈亏的能力 p=0.973。**这是本系统最昂贵的一个无效门禁。**

### 裁决 4 · 「降杠杆能不能改善」→ **不能，乙方认输**

手续费与风险敞口**同比例随杠杆缩放**，比值恒定：实测每个周期的「双边费 ÷ 保证金」都精确等于 **2.00%**
（因为杠杆一律顶在 ~20×）。且 t = mean/sd×√n 对杠杆不变，降杠杆也买不到统计功效。

**降杠杆只能让你输得慢，不能让你少输。** 它是资金存活问题，不是期望值问题，本计划不列入。

### 裁决 5 · 「有没有正期望子集」→ **现有样本回答不了，且必须放弃逐格检验**

12 套体系里**只有 7 套真正成交过**（gann / gap / martingale / arbitrage / volatility 五套零成交），
实际网格 42 格、有数据 35 格、n≥5 仅 23 格，格子中位数约 10 笔。

- 期望为正的仅 3 格，**无一接近显著**（最好的 oscillator|15m p=0.384；且其 +4.52U 毛利中 32% 来自单笔，
  去掉那一笔只剩 +1.46U/13 笔 = 噪声）
- 负的一侧：**9 格过 BH-FDR(0.05)，2 格过 Bonferroni**（triple_rsi|5m p=1.8e-6、triple_rsi|15m p=1.3e-4）

> **T1 纠偏后已重跑，核心结论抗住了**：正期望格子 3 → **4 个**（新增 oscillator|30m），
> 显著为负 9 → **10 个**，而**「正期望且 p<0.05」仍然是 0 个**。
> oscillator 包揽正期望前三名——这是它在四个互相独立的口径上第四次排第一。

**这个不对称是决定性的**：若仅仅是「样本不够所以什么都看不出来」，正负两侧应当同时不可见。
实际是负侧一清二楚、正侧一个都没有。这把尺子对大效应是好用的，而它量到的所有大效应全是负的。

**样本量缺口**（80% 功效，edge = 1% ROM/笔 = 0.0952U，**sd 取 T1 纠偏后的 0.9098U**）：

| 目标效应 | α=0.05 | Bonferroni×72 | 计入 5.7× 相关性折扣 | 按当前速率 |
| --- | --- | --- | --- | --- |
| +1% ROM/笔 | **718 笔/格** | **1,639 笔/格** | 4,100–9,300 笔/格 | **数十年** |
| +2% ROM/笔 | 180 笔/格 | 410 笔/格 | — | — |

> **口径订正记录**：辩论过程中曾先后出现「971 / 2,216」与「1,060 / 708」两组数字，
> 两版都用了**未经 T1 纠偏**的 sd=1.1119U。纠偏后 sd 降到 0.9098U，上表为统一重算结果。
> **结论方向不变（仍差两个数量级），但数字以本表为准。**

**在成交层逐格找赢家这条路已被算术堵死，不是「还没找到」。**

> **但这个「84 年」有一个隐含前提，而它是错的**——它默认证据只能来自**成交**。
> 见裁决 7：证据可以来自信号层，那里的数据快 772 倍，且已经躺在库里 17 天了。

### 裁决 6 · 真正的病因：**不是方向选错，是没有方向信息**（推翻双方开局立场）

这是全部辩论中唯一**两人开局都说错了**的一条，也是最重要的一条。

甲方原本给出一记决定性打击：**毛价格位移**（不含费、不含杠杆的纯方向能力）
均 -0.1096%、t=-3.77、p=0.0002——即「信号在付出第一分钱之前就已统计显著地选错方向」。

**但这个数字用的是 `exit_price`，而止损单的 `exit_price` 正是被 T1 那个不对称结算劣化过的价格。**
把 240 笔止损改回按**计划止损位**结算后重算：

| 口径 | 毛位移 | t |
| --- | --- | --- |
| 台账原值 | -0.1096% | **-3.77** |
| T1 纠偏后 | **-0.0478%** | -1.97 |
| T1 纠偏 + 独立赌注口径（n_eff=76） | **-0.0229%** | **-0.40** |

**那 0.1096 个百分点里有 0.0618（56%）是模拟器结算不对称造出来的。**
套上甲方自己坚持的独立赌注口径，**t=-0.40，与抛硬币区分不开**。

分周期同样全部不显著：5m -0.0116%(t=-0.60)、15m -0.0105%(t=-0.20)、
1h -0.1053%(t=-0.93)、4h -0.2411%(t=-1.22)。

**所以正确的诊断是第三种：**

- 不是甲方说的「信号选错方向」——纠偏后 p=0.0490 抵在边缘，独立赌注口径 t=-0.40，说不成立
- 不是乙方说的「信号有效但被执行层漏血」——已被裁决 2、3 驳倒
- **而是：信号没有可测到的方向信息（t=-0.40），而 0.1% 的固定过路费把「没信息」确定性地转成了「稳定亏损」。**

一个方向完全随机的系统，每笔交 0.1% 过路费，就会稳定亏 0.1%/笔。这与信号好坏无关，是算术。

> **但承重的那条结论没塌**：把纠偏后的毛位移拿去对 **0.10% 过路费地板**检验，
> 仍为 -0.1478%、**t=-6.11**。「没有体系跨得过过路费地板」这条**不受纠偏影响**。
> 变的是「信号有多差」，没变的是「过路费有多高」。

**⚠ 一句必须原样保留的精确化（防误读）**：

> **「测不出方向信息」与「测得出远低于地板」是两件事，而且同时成立。**

同一组独立赌注数据：对 **0** 检验 t=-0.40（区分不开）；对 **0.10% 地板** 检验
**t=-2.15、p=0.0175（显著低于）**，CI 上界 +0.0893% 仍在地板之下。

这条防的是一种很自然的误读——有人看到 t=-0.40 会说「证据不足，再跑跑看」。**不是。
关于「能不能赚钱」的判定已经做出来了，而且是否定的；没做出来的只是「信号到底是零还是微负」，
而那个问题在商业上不重要。**

**副产品（值得记一笔）**：剔除伪影后分体系毛位移净于 0.1% 费用——
`oscillator +0.0219%`（唯一点估计跨过地板）、rule123 -0.0697%、chanlun -0.1370%、
dow -0.1600%、elliott -0.1850%、triple_rsi -0.2015%、turtle -0.3855%。
oscillator 在成交层毛位移、信号层命中率（52.13%）、纠偏后净于费三个**互相独立的口径**上都排第一，
**这不足以让它上机，但足够让它当 T9 信号层试验的首选标的。**

> **这条同时把 T1 钉成第一优先级**：不修结算口径，你会把一个模拟器缺陷读成 p=0.0002 的信号结论。
> 辩论双方**各自独立地**犯了这个错——甲方把它当信号证据，乙方把它当真实滑点。

### 裁决 7 · 出路：把评估从成交层挪到信号层（84 年 → 数月，且零成本零风险）

「84 年」成立的前提是**证据只能来自成交**。这个前提是错的。

库里已有 **`twelve_signal_changes` 表：50,005 条，9 个标的，跨 17.1 天**（已实地核验）：

| 项 | 实测 |
| --- | --- |
| 标的 | ADA 9538 / ETH 9026 / DOGE 6873 / SOL 6570 / BTC 5963 / BNB 5960 / XRP 5954 / XAU 94 / SNDK 27 |
| 方向分布 | bullish 16,985 / bearish 16,538 / neutral 16,482 → **方向性 33,523 条** |
| 体系覆盖 | 11 套（含成交层零成交的 gann 2,896 / arbitrage 4,042 / volatility 1,571 / gap 38） |
| 关键列 | `ts, symbol, tf, system, new_direction, price` —— 即「带时间戳与价格的方向预测」 |

**方向性信号 1,960 条/天，对比门禁后 2.54 笔/天——快 772 倍。**
而且它不花手续费、不占保证金、不被任何门禁筛选，**门禁开着也照样累积**。

同时它解答了另一件事：**那 5 套「零成交」的体系不是没有信号，是信号全被成交层门禁挡在外面了**
（gann 近 2,900 条信号、arbitrage 4,042 条）。它们从未被评估过，不是因为不产出，是因为看不见。

**探索性初跑（尚不可作结论，见 T9 的预登记要求）**：池合均毛位移 -0.0097%，
95%CI [-0.052%, +0.033%]，对 0.10% 地板 t=-5.05、p=6.0e-05。8 个体系中 **0 个达标、5 个已可淘汰、3 个待定**。

**到判定还需要多久**——⚠ **本节初版严重低估，已按 T9 实跑结果订正**：

| 版本 | 估计 | 错在哪 |
| --- | --- | --- |
| 初版（作废） | 再 27 天 ≈ 4 周 | 按**记录条数**算，默认每条信号是一个独立观测 |
| **实测（采用）** | **oscillator 还需 103 天** | 按预登记的**按日聚类**算，n_eff = **天数**不是条数 |

**根因**：同一天的上千条信号受同一个日间共同因子支配，不构成独立证据。
`jarvis_signal_floor.py` 的原话：「日均条数增加不会缩短上表——约束是日间共同因子，只有累计天数能解。」

**所以「信号层快 772 倍」这个说法要精确化**：它快在**不花钱、不担风险、不受门禁限制、覆盖 11 套体系**，
**不快在把统计时钟拨快**。真正能缩短日历时间的只有一件事：**增加互相独立的「标的-日」观测**，
也就是扩标的（见 T4，硬依赖专用出口 IP）。

**一笔单都不用多开** —— 这一点不变，仍是信号层相对成交层的决定性优势。

> **一条必须写死的方法论警告**：同一批数据，朴素逐笔算命中率得 49.39%、z=-2.00、p=0.046（看着显著）；
> **按天聚类后 t=-0.52、p=0.61（不显著）——朴素算法把显著性虚高了约 4 倍。**
> 成因是重叠视界 + 同时刻多体系同向下注。换数据源**不代表**躲开了 n_eff 问题，聚类口径必须预登记。

---

## 三、新增共识：一个与信号无关的结构性必亏区

这是两派唯一**完全一致**的发现，也是全部结论中置信度最高的一条——因为它是会计恒等式，不是统计推断。

双边手续费按名义收取，恒等于名义的 **0.1%**。因此：

```
过路费 ÷ 风险预算 = 0.1% ÷ 止损距离%      （与杠杆、与信号、与行情全部无关）
```

按止损距离分档实测（总额比口径）：

| 止损距离 | 笔数 | 过路费 | 净额 | 剔摩擦 | 胜率 |
| --- | --- | --- | --- | --- | --- |
| **< 0.1%** | **95** | **2.006 R** | **-3.117 R** | -0.115 R | **9.5%** |
| 0.1–0.2% | 77 | 0.681 R | -1.265 R | -0.034 R | 31.2% |
| 0.2–0.5% | 130 | 0.297 R | -0.542 R | -0.100 R | 28.5% |
| 0.5–1.0% | 84 | 0.143 R | -0.391 R | -0.125 R | 35.7% |
| > 1.0% | 47 | 0.064 R | -0.308 R | -0.203 R | 42.6% |

三件事同时成立：

1. **95 笔（21.9%）在下单那一刻就已注定亏损**——止损距离不足 0.1%，光过路费就是风险预算的 2 倍，
   行情走到止盈也得先赚回两倍止损。实测胜率 9.5%。
2. **但剔摩擦后，窄止损反而是信号最好的一档**（-0.034R @0.1–0.2% vs -0.203R @>1.0%）。
   所以「把止损拉宽」省下的过路费，会被信号质量下降抵掉一大半——**这正是裁决 3 里 A/B 期望相等的机理。**
3. **没有任何一档是正的。** 过路费最低的一档（0.064R）仍为 -0.308R。只保留过路费<0.2R 的 131 笔，
   仍为 -0.345R，剔摩擦区间 [-0.247R, +0.020R]。**不存在靠筛选就能得救的子集。**

**S1 门禁的真正错误因此可以精确表述**：它的**数字蒙对了**（5m 阈值 0.5% 恰好对应「过路费 = 20% 风险」），
但**理由错了**（「噪声带」假说被裁决 3 否定）、**手段更错**（选择拒单而非改写）。

---

## 四、任务总表

| 编号 | 状态 | 任务 | 解决什么 | 优先级 | 预估 |
| --- | --- | --- | --- | --- | --- |
| T1 | ⬜ | 止损结算口径纠偏（轮询粒度 → 挂单语义） | 51.35U 悲观偏差 | P0 | 1d |
| T2 | ⬜ | S1 由「拒单」改「改写止损 + 同比例缩仓」 | 恢复 84% 样本流 | P0 | 1.5d |
| T3 | ⬜ | 过路费地板门禁（判据换成 fee/R） | 21.9% 结构性必亏 | P0 | 0.5d |
| T4 | ⬜ | 多标的扩展（1 → 20 个 symbol） | 采样速率 ×20 | P1 | 2d |
| T5 | ⬜ | 历史回放回测台（一次性生成万级样本） | 绕开 84 年 | P1 | 3d |
| T6 | ⬜ | 分层/部分合并估计替代逐格检验 | 让归因在小样本下可用 | P2 | 2d |
| T7 | ✅ | 零成交五套体系处置 | 12 套实为 7 套 | P2 | 0.5d |
| T8 | ✅ | 振幅准入门槛（毛位移 > 0.1%） | 裁决 6 的落地闸门 | P0 | 1d |
| **T9** | 🟡 | **信号层地板评估台（含预登记）** | **裁决 7 的落地工具** | **P0·最高** | 2d |
| **T10** | ⬜ | **采集覆盖率补齐（常开机器 + WS 连续落带）** | **T9 结论可信的前置** | **P0** | 2d |

> 公共纪律：所有开关默认值必须在 `jarvis_config.py` 的 **DEFAULTS + GROUPS + BOUNDS 三处登记**；
> 被门禁处理的信号**不静默丢弃**，一律落 `reject_reason` 或 `context_tags` 可复盘；
> 每任务扩 `_twelve_sim_smoketest.py` 用例并保持 `_wyckoff_smoketest.py` 48/48、
> `_supply_demand_smoketest.py` 27/27 零回归；不新增出网请求；只 commit 本次相关文件，不 push。

---

## 五、逐任务契约

### T1 · 止损结算口径纠偏（P0）

**为什么排第一**：它不是为了赚钱，是为了**修正测量仪器**。当前仪器系统性地把成绩记差了，
所有基于历史数据的判断都建立在这个偏差之上。

**根因**（已定位，非推测）：`jarvis_twelve_trader.py:1455 _exit_check()`

```python
# :1493  多头止损
return ("sl", min(sl, price))    # 缺口向下：按更差的快照价结算
# :1499  多头止盈
return ("tp", tp)                # 恒按计划位结算
```

引擎以 `run --interval-min 5` 轮询（见 `start-all.sh:44`）。两次轮询之间价格可能已远越过止损位，
唤醒时按「止损位与当前快照价中更差的那个」结算；而止盈**恒按计划位**结算。
**这是一个不对称的悲观偏差**：止损吃满滑价、止盈一分不让。实测代价 51.35 U（占净亏 28%）。

**一句 SQL 定案（甲方独立验证，已排除「真实跳空」这个竞争解释）**：

```
exit_reason |  n  | 结算恰在计划位 | 均超额偏离 | max
tp          |  79 |  79 (100%)     | 0.00000%   | 0.00000%
sl          | 240 | 106 (44%)      | 0.11155%   | 2.04470%
```

**79 笔止盈全部精确等于计划位、方差为零；240 笔止损里 134 笔差于计划位、0 笔优于计划位。**
真实跳空是双向的——若是市场行为，止盈方向也该有跳过头的情形。79/79 零方差在市场里不可能发生。
**这是代码单边，不是行情。**

佐证：穿透幅度与 `holding_minutes` 相关 r=+0.230，且分周期单调递增
（5m 0.0592% → 15m 0.1481% → 1h 0.2095% → 4h 0.2256%）——**周期越长、检测滞后越久、穿透越大，
这是滞后的签名，不是市场的签名。**

**⚠ 还有一个更重的缺陷（同一函数，独立于上面那条）**：
`:1476-1477` 触发判定用 `eff_lo = min(lo, price)`，其中 `lo` 是**开仓以来所有已收盘 bar 的最低价**；
但 `:1493` 结算用的是**当前快照价**。若某根 bar 的影线几小时前触到止损却未被检出，
等这一轮检出时会用**此刻的价**结算。**所以滞后根本不受 5 分钟轮询间隔约束。**
证据：穿透 P99=1.82%、max=2.04%，而 ETH 5 分钟波幅 P99 仅 0.5132%——**超出 4 倍，轮询解释不了。**

**金额拆分**：仿真穿透 51.35U，其中按真实滑点（0.02%）应付约 9.20U，
**伪影约 42.14U（占 82%）**。剔伪影后总净亏约 -141.02U（仍深度为负）。

**它有多要命（这就是它排第一的原因）**：辩论中双方**各自独立地**被它骗过一次。
甲方拿含偏差的 `exit_price` 算出「毛位移 -0.1096%、p=0.0002、信号方向显著错误」，
乙方把同一笔钱当成真实市场滑点写进执行层漏血清单。纠偏后 t 从 -3.77 掉到 -0.40（见裁决 6）。
**不先修这里，任何基于历史台账的结论都可能是在解读一个模拟器缺陷。**

**改动（注意：不是「让止损也按计划位结算」——那样会矫枉过正）**

> **为什么不能简单去掉不对称**：现实中止盈是**限价单**，恰好成交在限价；
> 止损通常是**止损市价单**，触发后按市价成交，**确实会滑**。
> 所以代码的不对称**方向上是对的**，它镜像了限价 vs 止损市价的真实差异。
> 错的是**量级**（真实滑点 0.01–0.03%，仿真 0.1116%，约 6 倍）与**时刻错配**。

1. **修时刻错配（这一条最重要）**：结算价必须取**触发那根 bar 的价**，而不是当前快照价。
   `_exit_check` 需要返回触发所在 bar 的标识，由 `_do_close` 按该 bar 结算。
2. **换结算公式**：`触发位 + 常数滑点`，滑点方向恒为不利方，默认 **0.02%**，
   配置键 `twelve_sl_slippage_pct`。止盈保持按计划位（限价语义正确，不动）。
3. **真跳空仍需诚实**：若触发 bar 的 `open` 已越过止损位，按 `open` 结算（真实缺口，不叠加常数滑点）。
   判据是「bar 开盘即越过」而非「快照价越过」——前者是市场缺口，后者是检测延迟。
4. `jarvis_config.py` 三处登记：DEFAULTS `twelve_sl_slippage_pct=0.02`；GROUPS 归 `"sim"`；
   BOUNDS `(0, 0.5)`。另加 `twelve_sl_fill_mode`：`bar`（新默认）/ `poll`（回退档，保持现状）

**验收**：
- `_twelve_sim_smoketest.py` 新增 5 例：检测滞后按触发 bar 结算 / 常数滑点方向恒不利 /
  真跳空按 open 结算且不叠滑点 / `poll` 回退档行为与改前逐字节一致 / 止盈路径零变化
- 全量 smoke 零回归；`py_compile` 通过
- 改后新平仓的 sl 单，`|exit_price − stop_loss| ÷ entry` 的 **P99 < 0.05%**（当前 P99=1.82%）
- 穿透幅度与 `holding_minutes` 的相关系数降到 **|r| < 0.05**（当前 +0.230）——
  这一条是「时刻错配已修好」的直接指标

**回滚点**：`twelve_sl_fill_mode="poll"` 即完全恢复旧行为，无需回滚代码。

**停损条件**：若实测发现 >20% 的止损单确属真跳空（bar open 已越过），说明这不是轮询问题而是流动性问题，
T1 收益将大幅缩水——此时停止本任务，改为在 T4 中优先选择深度更好的标的。

---

### T2 · S1 由「拒单」改「改写止损 + 同比例缩仓」（P0）

**为什么**：裁决 3 已证明止损距离不携带期望值信息，而 S1 拿它拒掉了 532 笔（65% 的拒单量），
把成交速率从 146 笔/天打到 0。**它付出了全部样本，换来一个 p=0.973 的改善。**

**但不能简单删掉 S1**：第三节证明过路费地板是真实存在的（<0.1% 档必亏）。
正确做法是**把「拒绝」换成「改写」**——保住样本，同时让每笔交易过路费可控。

**改动**：`jarvis_twelve_trader.py:873 _risk_gate` 前插入 `_widen_stop_to_floor()`

```
若 SL 距离 < 地板：
    SL ← 地板距离（按 entry 重算价位）
    qty ← qty × (原 SL 距离 ÷ 地板距离)     # 保持 1R 名义风险恒定
    落 context_tags='sl_widened'，signal_log 留痕原值与新值
    重算 RR，交由 S2 判定；RR 不达标才拒 rr_too_low
```

**关键**：缩仓是**必须的**，否则拉宽止损等于放大单笔风险。缩仓后 1R 不变，
过路费绝对额同比例下降，`fee/R` 落到地板值。

**配置**（`jarvis_config.py` 三处登记）：
- `twelve_sl_gate_mode`：`rewrite`（新默认）/ `deweight` / `reject`（旧行为回退）
- 现有 `twelve_min_sl_pct` 保留但**语义降级**为 rewrite 的目标地板，不再用于拒单

**参照实现**：D6 已把 S3 熔断、S4 置信档、S5 逆势三道硬拒改成 mode 开关，
`GATE_TAG_FACTORS`（`:253`）与 `_apply_gate_tags`（`:1132`）是现成设施，照抄接线。
两个调用点：`:1984`（计划触达成交）与 `:2358`（直开）。

**验收**：
- 窄止损单不再产生 `sl_too_tight` 拒单，改为成交且 `context_tags` 含 `sl_widened`
- **1R 守恒**：改写前后 `|entry−sl|×qty` 相对误差 < 0.5%（单测断言）
- `twelve_sl_gate_mode="reject"` 时行为与改前逐字节一致（回归档）
- 上线 48h 内成交速率恢复到 **≥ 50 笔/天**（对照 8/05 的 146 笔/天）

**回滚点**：`twelve_sl_gate_mode="reject"`。

**停损条件**：若恢复样本后 30 日净亏速率**超过 8/05 前水平的 1.5 倍**（即 > 66U/天），
说明改写引入了新的出血点，立即切回 `deweight` 档并复盘。

---

### T3 · 过路费地板门禁（P0）

**为什么**：把第三节那条会计恒等式变成代码。当前 S1 用「止损距离绝对值」当判据，
判据错了——正确判据是 `fee/R`，它才是与杠杆、周期、标的全部无关的不变量。

**改动**：`_risk_gate` 中新增计算

```
toll_ratio = (2 × fee_pct) ÷ sl_dist_pct        # = 0.1% ÷ SL距离%
若 toll_ratio > twelve_max_toll_ratio(默认 0.20)：
    交由 T2 的 _widen_stop_to_floor 改写；改写后仍超标 → 拒 toll_too_high
```

**默认值依据**：0.20 对应止损距离 0.5%，恰是实测「过路费 0.143R」那一档的入口，
且与 S1 原 5m 阈值数值重合（但**理由完全不同**：不是噪声带，是过路费占比）。

**验收**：
- 新台账中 `fee/R > 0.2` 的成交笔数占比 **< 2%**（当前为 70%）
- 「止损距离 < 0.1%」的成交笔数归零（当前 95 笔 / 21.9%）
- 归因接口 `GET /api/twelve/attribution` 增加 `toll_ratio` 分档维度

**回滚点**：`twelve_max_toll_ratio = 999` 即关闭。

---

### T4 · 多标的扩展（P1）

**为什么**：甲方已证明——**唯一能真正提高有效样本速率的杠杆是标的数**，不是时间、不是杠杆、不是加信号。
当前 433 笔压成 76 个独立赌注，正因为全部押在同一个 ETH 上、平均 17.3 个仓位同时开着。
扩到 20 个流动性相当的标的，独立观测速率约 ×20，横截面相关性远低于同标的并发注。

**改动**：`watchlist` 从 `[BTCUSDT, ETHUSDT]` 扩至 20 个 USD-M 永续；
`jarvis_twelve_trader` 的槽位与钱包按 symbol 隔离（现已按 symbol×tf×system 建槽，需核对余额池是否共享）。

**硬前置**：**当前币安 IP 处于限频封禁中**（`task-53d4d479` 尚未结案）。
标的数 ×10 会把出网请求同比例放大，**封禁排查必须先结案**，否则本任务会直接压垮数据链路。

**验收**：
- 20 标的全部有 K 线与盘口数据，`/api/kline` 无 `stale:true`
- 出网权重占币安配额 **< 50%**（复用 `jarvis_net.py` 的权重计费限流）
- 独立赌注速率（按小时×方向×标的归并）达到 **≥ 30 个/天**

**停损条件**：若扩到 5 个标的时出网权重已超配额 50%，停止扩容，转 T5。

---

### T5 · 历史回放回测台（P1）

**为什么**：这是**唯一能在数周内拿到万级样本**的手段。T4 把 84 年压到 4 年，仍然不够；
历史回放可以一次性把 12 套信号在 20 个标的 × 2 年历史上重跑，直接产出 10⁴–10⁵ 量级样本。

**关键约束（否则白做）**：回放引擎必须与实盘引擎**共用同一套判定代码**，
包括 T1 的结算口径、T2 的改写逻辑、T3 的过路费地板、以及手续费与资金费计提。
若回测口径与实盘不一致，得到的结论无法迁移——这是回测最常见的失败方式。

**改动**：新建 `jarvis_twelve_replay.py`；复用 `jarvis_twelve_systems.run_all()` 与
`jarvis_twelve_trader` 的 `_exit_check` / `_risk_gate` / `_do_close`（需先把这三者从数据库耦合中解出，
改成可注入 bar 序列的纯函数形态）。

**验收**：
- **口径一致性**：取实盘已发生的 433 笔，用回放引擎重跑同期同参数，逐笔 pnl 相对误差 < 1%
  （这一条不过，后面的结论全部作废）
- 单次全量回放（20 标的 × 6 周期 × 7 体系 × 2 年）在 30 分钟内跑完
- 产出样本量 ≥ 20,000 笔

**停损条件**：若口径一致性验收（433 笔重放）无法收敛到 1% 以内，**停止本路线**——
说明实盘引擎存在未被建模的状态依赖，应先做 T6 而非继续投入回测。

---

### T6 · 分层估计替代逐格检验（P2）

**为什么**：甲方已证明逐格检验（42 格各自独立做假设检验）在任何可及的样本量下都不成立，
且 32 个格子在纯随机下至少一格出现 p<0.05 的概率是 **80.6%**——继续逐格找赢家只会不断制造假发现。

**改动**：把归因从「每格独立估计」改为**部分合并（partial pooling）的分层模型**：
先估计全局均值，再按体系、周期两个维度估计偏移量，小样本格子自动向全局均值收缩。
落地位置：`GET /api/twelve/attribution` 增加 `?estimator=hierarchical`。

**验收**：
- 对当前 433 笔跑分层估计，输出每格的收缩后期望与可信区间
- **必须同时输出每格的「样本充分性」标记**，n 不足时前端明确显示「不可判定」而非给一个数字
- 与逐格朴素估计对比，验证收缩方向正确（小样本格子向全局收敛）

---

### T7 · 零成交五套体系处置（P2）

**事实**：12 套里 gann / gap / martingale / arbitrage / volatility **8 天零成交**。
它们要么信号条件永不满足，要么在门禁前就被过滤，要么实现有 bug。

**改动**：先诊断（各自的 `_insufficient` 返回原因分布），再决定「修复」或「明确下线」。
无论哪种，都要把「12 套」这个对外口径改成真实数字——**文档与界面上的 12 是虚的，会误导后续所有判断**。

**验收**：五套各给出零成交根因（一句话 + 代码行号），并在配置中显式标记 enabled/disabled。

**✅ 诊断结论（2026-08-13 已交付，DB 实测 + 代码定位双证据）**：

| 体系 | 根因（一句话） | 证据行号 |
| --- | --- | --- |
| volatility | 设计上恒 neutral——三个返回分支全是 neutral（注释明言「波动率体系本身不判涨跌」），trader 对 neutral 恒跳过 | `jarvis_twelve_systems.py:334-340`；`jarvis_twelve_trader.py:2322` |
| martingale | 设计上恒 neutral——「资金管理系统而非方向信号」，且 run_all 不传 trade_history；signal_changes 全窗口 0 条方向记录 | `jarvis_twelve_systems.py:646-662` |
| arbitrage | 设计上恒 neutral——「方向中性策略」，触发也输出 neutral；4,102 条变更全为 neutral | `jarvis_twelve_systems.py:780-821` |
| gann | **诊断时为实现缺陷**：方向信号不带 trade_plan（`_sig` 调用缺 trade_plan 参数），槽位又无 SL/TP 配置 → `_resolve_entry_params` 返回 None 被**静默跳过（无任何留痕）**；ETHUSDT 有 299 条方向变更、仅 13 笔 rejected（tf_gate 11 + counter_trend 2，全是 `_pre_gate` 在此之前拦的）。**后续处置**：信号篇 P2-5（commit 0bbf4a9，2026-08-13）已把时间窗方向**降级恒 neutral**（方向证据薄弱），此后 gann 与恒 neutral 三套同为结构性零成交 | 产（降级前）`jarvis_twelve_systems.py` signal_gann；拦 `jarvis_twelve_trader.py:942-943, 2356-2357` |
| gap | **信号侧零输入**：成交标的 ETHUSDT 上方向变更为 0 条（全部 19 条方向信号在 ADA/DOGE/XRP）——缺口判定要求 `cur_l > prev_h + 0.1×ATR`，高流动性币 24h 连续交易几乎不留缺口 | `jarvis_twelve_systems.py` signal_gap 缺口判定 |

- 恒 neutral 三套是**结构性零成交**（永远不会开仓），非 bug；「12 套」口径实为
  **注册 12 / 具方向能力 8（P2-5 gann 降级后）/ 真实成交 7**。
- 配置显式标记：`jarvis_config.twelve_system_enabled`（0=设计上/降级后不产生方向信号，
  非人工停用；volatility/martingale/arbitrage/gann=0，其余=1）。
- 界面口径：`GET /api/twelve/attribution` 恒增 `systems_note`（registered/traded/
  always_neutral/direction_downgraded/enabled_map），前端按此显示真实数字。
- 遗留（交 trader 侧任务）：`_resolve_entry_params` 返回 None 的静默跳过建议补
  reject 留痕（当前无痕不可复盘——gann 若未被 P2-5 降级，此缺陷会继续吞掉一切
  「有方向无点位」的信号且不留任何痕迹）。

---

### T8 · 振幅准入门槛（P0）

**为什么**：裁决 6 证明这套信号没有可测方向信息，而 0.1% 过路费把「没信息」转成「稳定亏损」。
于是有一条**不需要等上千笔成交就能判定**的准入线——因为它问的是**振幅**（信号能不能预测足够大的位移），
不是**优势**（信号方向对不对）。振幅是低方差量，小样本即可定案；优势是高方差量，需要千级样本。

**门槛定义（写成公式，不要写死数字）**：

```
地板 = 2 × 单边费率（twelve_sim_fee_pct）
     = 0.1000%  （当前 fee_pct=0.05 时）

准入条件：该信号在 T1 纠偏口径下的预期毛价格位移，
          按独立赌注聚类、经 11 重 Bonferroni 校正后，
          单侧检验拒绝「毛位移 ≤ 地板」
```

> **地板必须随费率走**。写死 0.10% 的话，将来换了费率档位（更低手续费等级 / maker 返佣）
> 这条栏杆就错位了——而「换更低费率通道」恰恰是本计划停损条件里给出的出路之一。

**要多少样本才能判（T1 纠偏后独立赌注 sd=0.4991%，相关性压缩 5.7×，80% 功效，单侧 α=0.05）**：

| 真实毛位移 | 超出地板 | 需独立赌注 | 折合原始信号记录 | 信号层天数 |
| --- | --- | --- | --- | --- |
| 0.125%（1.25× 地板） | +0.025% | 2,464 | 14,039 | 7.2 天 |
| **0.150%（1.5× 地板）** | **+0.050%** | **616** | **3,510** | **1.8 天** |
| 0.200%（2× 地板） | +0.100% | 154 | 878 | 0.4 天 |
| 0.300%（3× 地板） | +0.200% | 39 | 223 | 0.1 天 |

**加上 11 重 Bonferroni 校正（样本量 ×1.85），以 1.5× 地板为备择假设：
≈ 1,140 个独立赌注 ≈ 6,500 条原始信号记录 ≈ 信号层 3.3 天。**

**这个标准今天就能落地**——信号层现有分体系样本量：
triple_rsi 6,483、oscillator 6,424、dow 3,482、chanlun 3,414、rule123 2,557、
elliott 1,931、gann 1,314、turtle 1,141。

> **oscillator 与 triple_rsi 已越过 6,500 门槛，今天即可判定。** 其余体系再累 2 天到一周。
> 对比交易层的 84 年。

**这条同时适用于现有 7 套和任何新增信号。** 过不了这条线的信号，无论方向多准都运不出利润——
因为它预测的行情幅度还不够付过路费。

**落地**：
1. `GET /api/twelve/attribution` 增加 `gross_displacement` 与 `net_of_toll` 两个指标（按 T1 纠偏口径）
2. 每个 `system × tf` 给出「振幅达标 / 不达标 / 样本不足」三态
3. 不达标者进入观察名单，**不自动停用**——停用决定留给用户

**验收**：
- 对当前 433 笔跑出 7 套 × 5 周期的振幅达标表
- **必须标注每格的功效**：达到 80% 功效判定所需笔数，样本不足时显示「不可判定」而非给结论
- 与 T3 的过路费地板交叉验证：振幅不达标的格子，其 `fee/R` 应当同步偏高

**为什么它是 P0 而不是 P2**：它是唯一一条**当前样本量就能回答**的策略性问题。
其余关于「哪套信号好」的问题都要等 T4/T5 把样本堆起来，只有这条现在就能出结论。

**停损条件**：若 7 套 × 5 周期全部振幅不达标，则**不要再往这套信号体系里投入工程量**——
结论就是「现有信号预测的行情幅度普遍付不起过路费」，此时唯一选项是换更低费率的通道
（更低手续费等级 / maker 挂单返佣）或换更长周期，而不是继续调信号。

**✅ 落地记录（2026-08-13 已交付）**：

- 计算模块 `jarvis_twelve_amplitude.py`（纯函数、不连库；口径预登记 A1-A7 锁在模块头：
  T1 纠偏 / 动态地板 / 同小时×同方向赌注聚类 / Bonferroni 调整区间双向约束 /
  终局优先判定次序 / 80% 功效标注 / fail 仅进观察名单）；
  接线 `GET /api/twelve/attribution` 恒增 `amplitude` 键
  （gross_displacement / net_of_toll / 三态表 cells / watch_list / toll_ratio 五档）。
- 用例 `_twelve_amplitude_smoketest.py` 21/21；`_wyckoff` 48/48、`_supply_demand` 27/27 零回归。
- **首轮达标表（436 笔 / 77 独立赌注 / 35 格中 32 格参检，Bonferroni m=32）**：
  **0 达标（pass）/ 3 不达标（fail）/ 32 不可判定（insufficient）**。
  不达标：chanlun|5m（-0.0063%）、triple_rsi|5m（-0.0135%）、triple_rsi|15m（-0.1540%），
  已进观察名单未自动停用——与裁决 5「负侧过 Bonferroni 的正是 triple_rsi|5m/15m」双口径互证。
- 全局对账（vs §一取证基线，433→436 笔差 3 笔新数据）：纠偏后毛位移 **-0.0505%**
  （基线 -0.0478%）、独立赌注 **77**（基线 76）、台账原值对照 **-0.1126%**（基线 -0.1096%）、
  赌注口径对地板 **t=-2.36**（基线 t=-2.15）——方向与量级全部复现。
- toll_ratio 五档与 §三实测表逐位对上（>1.0 档 95 笔 / toll 2.006R / 净 -3.117R / 胜率 9.47%）。
- 与 T3 交叉验证：3 个 fail 格全在 5m/15m 短周期（窄止损、toll_ratio 高档位聚集），方向一致。
- 判定口径较契约原文的一处显式偏差：契约写「单侧检验拒绝『毛位移 ≤ 地板』」（只定义了达标），
  落地采用 T9 P7 的对称口径——**不达标（fail）同样要过 Bonferroni 调整区间**（上界 < 地板），
  避免反方向挑樱桃；样本不足时输出「不可判定」及 80% 功效所需笔数，不给结论。

---

### T9 · 信号层地板评估台（P0 · 全表最高优先级）

**为什么它排在所有任务之前**：其余任务都在改善「怎么交易」，只有它回答「**这些信号到底有没有用**」——
而且它把答期限从 84 年压到 4 周，代价是零成交、零手续费、零风险。

**命题（一句话）**：

> 每个体系「按其预测方向的平均毛价格位移」，能否显著超过 0.10% 的往返过路费地板？

这个命题把两派结论焊成一个可判定量：**0.10% 是乙方的确定性会计地板（按名义收、不可谈判），
毛位移是甲方的与杠杆无关量。** 达不到地板，任何杠杆、仓位、止损结构都救不了；
达得到，才轮得到讨论执行层。

**判定规则（必须在跑数之前预登记，写进代码注释与文档，事后不得更改）**：

按天聚类的 95% 置信区间：
- 下界 > 0.10% → **保留，允许上机**
- 上界 < 0.10% → **淘汰**
- 跨 0.10% → **继续观察，不得上机**

**五条预登记项（漏一条这个台子就不可信）**：

1. **视界定义**：每个 TF 的持有视界必须事前固定（如 N 根 K 线），不得跑完再挑
2. **价格源**：用**真实 K 线**取视界末价，不得用「下一条 signal_changes 的 price」做近邻替代
   （探索性初跑用了近邻，命中率仅 79.8%，不合格）
3. **neutral 口径**：16,482 条 neutral 如何处理必须事前声明——排除本身就是一次选择，
   不声明就是又一次「只看筛过的那一半」
4. **多重比较**：11 个体系校 11 重、体系×TF 校 66 重。探索性初跑里 gann、oscillator 两个点估计
   高于地板，**8 个里冒出 2 个完全在随机预期内**，不校正就是挑樱桃
5. **聚类口径**：必须按天聚类。朴素逐笔会把显著性虚高约 4 倍（实测 p=0.046 → 0.61）

**落地**：新建 `jarvis_signal_floor.py`（只读分析脚本，不接入交易链路）；
数据源 `twelve_signal_changes` + 真实 K 线；输出「体系 × TF × 达标三态 + 功效标注」。

**验收**：
- 预登记文档先行提交，**其后才允许跑数**
- 输出表每格必须带「样本充分性」标注，不足时显示「不可判定」而非给结论
- 与成交层交叉验证：两层结论方向一致的体系标为「双口径确认」
  （目前 oscillator 两层都最好、elliott/turtle 两层都垫底——这种一致性本身就是证据）

**停损条件**：4 周后若**全部 11 个体系的日聚类 CI 上界都低于 0.10%**，
结论就是「现有信号预测的行情幅度普遍付不起过路费」。此时**不要再调信号、不要再加指标**，
唯一选项是换更低费率通道（更低费率等级 / maker 挂单返佣）或换更长周期。

**什么结果算甲方错**：任何体系日聚类 CI 下界稳定高于 0.10% → 该体系应上机，
「无一格有优势」的结论作废。
**什么结果算乙方错**：信号层与成交层结论系统性背离（信号层说行、成交层说不行）
→ 说明执行层确实在吃钱，乙方的执行漏血论重新成立。

---

### T10 · 采集覆盖率补齐（P0 · T9 结论可信的前置）

**为什么它从「锦上添花」升到「前置依赖」**：T9 首轮实跑发现，
真 bar 的可用率**按 UTC 小时从 30.0% 到 86.5%**（偏斜 2.9×），而这个偏斜造成的估计偏差是：

| 体系 | 池合 | 小时等权 | 差异 |
| --- | --- | --- | --- |
| oscillator | +0.0590% | +0.0112% | **-0.0478%** |
| elliott | -0.0635% | +0.0248% | **+0.0884%** |
| triple_rsi | +0.0074% | +0.0405% | +0.0331% |

**偏差最大 0.0884%，而地板才 0.1000%——覆盖偏斜与被测量同一数量级。**
后果：即使样本攒够了，**排名也不可信**（oscillator 与 elliott 在两种加权下会换位置）。

**根因（已实测定位，不是 IP 封禁）**：覆盖率峰值在北京时间 09:00–17:00、谷底在凌晨到早晨，
且没有任何一天接近 100%（区间 11%–59%）。**这是一台笔记本的作息，加上采集器是轮询驱动而非 WS 连续落带。**
（区分检验：封禁造成的缺口应成块且跨时区随机落位；开机时间造成的缺口应每天同一时段重复。
ETH 实测 303 个缺口块、中位 14 分钟、小时分布高度规律——属后者。）

**改动**：
1. 把 tape 采集搬到**常开机器**（VPS），不要在会休眠的本机上做常驻
2. 采集改 **WS 连续落带**，不再轮询驱动（WS 不占 REST 权重配额，顺带缓解封禁）
3. 落库补齐后回填历史缺口（若交易所允许拉历史 K 线，需等 IP 解封）

**与专用出口 IP 是同一笔投入**：VPS 同时解决「共享 IP 被封」与「机器会睡」两个根因，
也是 T4（扩标的）的硬前提。**三个痛点一个方案。**

**验收**：
- 连续 7 天覆盖率 **≥ 95%**，且按 UTC 小时的偏斜倍数 **< 1.2×**（当前 2.9×）
- T9 的「池合 vs 小时等权」差异收敛到 **< 0.01%**（当前最大 0.0884%）

**停损条件**：若 VPS 上线后覆盖率仍上不去，说明瓶颈在采集器实现而非机器，转去查 WS 断线重连。

---

## 六、明确「现在不该做什么」

| 不该做 | 理由 |
| --- | --- |
| **策略自动进化 / LLM 选股** | 项目自定纪律「负期望时自学习是亏损放大器」是对的。当前反馈信号既是负的、又被 72% 摩擦污染，进化只会更快找到更糟的局部最优 |
| **给 AI 加 skills / rules 让它"变聪明"** | 智能体上限受限于反馈信号质量。闭环没打通前（AI 说的话不影响下单、下单结果不回喂 AI），加 100 个 skill 只能得到一个更会讲话的错误系统 |
| **继续在 72 格里找赢家** | 已被算术堵死：单格需 4,100–9,300 笔成交。且 T1 纠偏后重跑 35 格——正期望 4 格、显著为负 10 格、**正且显著 0 格**，不对称本身就是答案 |
| **调 S1 阈值数值（路 1）** | 裁决 3：止损距离不携带期望值信息，阈值调到哪儿都一样（p=0.973） |
| **把降杠杆当「修复」** | 裁决 4：费用与风险同比例缩放，不改变括号符号。它是**止血**（降低燃烧速率、买时间），不是**修复**。可以做，但别指望它改善期望值 |
| **把止损外推当收益优化（原路 3）** | T2 采纳了「改写止损」，但**目的是保住样本 + 控过路费，不是提高胜率**。剔摩擦后宽止损档信号更差（-0.203R），不要指望它赚钱 |
| **重启生产前带着 16 个未提交文件** | 不知道跑的是哪版代码，任何数据都无法归因 |

---

## 七、阶段停损总闸

任何阶段触发以下任一条，**停止整条路线并回到用户处重新决策**：

1. **T1+T2+T3 全部上线满 30 天后**，风险归一净期望仍劣于 **-0.30 R**
   （对照当前 -0.518R；若摩擦修复的 72% 改善没有兑现，说明摩擦分解本身错了）
2. **样本恢复后满 30 天**，独立赌注口径下整体期望的 95% 置信区间**上界仍 < 0**
   （说明信号集合确定性为负，应换信号源而非继续优化）
3. **T5 口径一致性验收无法收敛**（433 笔重放误差 > 1%），说明实盘引擎不可建模
4. 任何阶段出现**单日净亏 > 8/05 前水平的 1.5 倍**

---

## 八、执行顺序与依赖

```
【主线·回答“信号有没有用”】不花钱、不开单、4 周出结论
T9（信号层地板评估台 + 预登记）──→ 9 天出池合结论 ──→ 4 周出分体系名单
                                                        │
                                                        ↓
                                          过地板的体系才进入下面这条线

【副线·回答“怎么交易才对”】只用来验证执行层，保持低流量
T1（结算口径）──┬──→ T8（振幅门槛，成交层口径）
                ├──→ T2（改写止损）──→ T3（过路费地板）
T7（零成交诊断）─┘

【后置·只有主线出了达标名单才值得做】
T6（分层估计） ← 归因可用
task-53d4d479（封禁排查，前置）──→ T4（多标的）──→ T5（历史回放）
```

- **T9 是全表最高优先级**：唯一能在 4 周内回答核心问题的任务，且零成本零风险
- **T1 必须在所有成交层结论之前**：它是历史台账的口径前提，已实证能把 p=0.0002 翻成 t=-0.40
- **T8 紧随 T1**：不依赖新样本就能出结论，与 T2 并行而非排队
- **T3 依赖 T2**：地板判据要靠改写来执行，单独上 T3 只会变成又一道拒单门禁
- **T4 / T5 降级**：信号层已有 7 个流动标的在跑，扩标的与历史回放不再是采样瓶颈的解药，
  留待「已有体系过了地板、需要更精细估计」时再做
- **T4 硬依赖封禁排查结案**：标的数 ×10 会同比例放大出网请求

> **一句话总结整份计划**：把钱从「拿真实成交去碰运气」挪到「拿信号层去碰地板」。
> 成交层保持低流量，只用来验证执行层修复；「哪个信号有用」全部挤到信号层去答——
> 那里的数据快 772 倍且不花钱。四周后你拿到的是一张「11 个体系谁过地板」的名单，
> 而不是 84 年后的一张。

---

## 九、两人共同验过、谁也没能推翻的三条

三轮辩论下来，双方各自推翻了自己的核心论据——乙方收回「降杠杆＝校准仪器」与「flip 漏血」，
甲方作废「信号方向显著为负」。剩下这三条是**两人独立复算、互相攻击后仍然站住**的：

1. **没有任何子集是正的**——乙方的 SL 分档表 5 档全负，甲方的 35 格 0 正 9 负
2. **0.1% 过路费地板与杠杆无关，而当前全体远在地板之下**——最严口径下 t=-2.15、p=0.0175
3. **「成本确定性 / 优势随机性」的管辖划分**——它既是本计划的骨架，
   也是 T8/T9 验收栏杆能成立的原因：**那条栏杆问的是振幅（确定性量），不是优势（随机性量）**

---

## 十、辩论存档

- 甲方（系统概率派 / Opus 5-8）：`task-a24f2187`
- 乙方（执行微结构派 / Opus 5-3）：`task-5e0e0be5`
- 双方各自公开认输的条目已并入第二节裁决，不单列
